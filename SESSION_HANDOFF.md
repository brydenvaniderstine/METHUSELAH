<!--
UPDATE RULE: Claude Code must update the "Last session summary" and "Next session priority"
sections at the end of any session that produces a finding, structural change, or decoder
update. Do not skip this step.

CREDENTIAL HANDLING: Do not paste live tokens/credentials into this file. Reference that a
credential exists and where it's stored — never the literal value.
-->

> North Star: pipeline/data/findings/why_not_conventional_trackers.md
> Read this before making any product decision this session.

# METHUSELAH — Session Handoff

This file is the single source of truth for picking up where the last session left off.
Updated by Claude Code at the end of any session that produces a finding, structural
change, or decoder update. If this file and a manually uploaded SESSION_HANDOFF_vN.md
conflict, this file takes precedence — it is version-controlled.

---

## Current build status

| Layer | Status | Notes |
|---|---|---|
| `web/` | Done | React PWA live. Business logic fully in `engine/` — the "not yet extracted" note here was stale; verified 2026-08-11 against the actual file, all three previously-documented `web/README.md` violations no longer exist. |
| `pipeline/` Track B | Active | 34 Gen3 pulls. 9 decoders validated, 14 partial. Decoders extracted to `pipeline/decoders/`. Walk experiment inconclusive (Oura app BLE contention). |
| `parsers/` | Not started | Skeleton exists. No parsers built. |
| `engine/` | Done | THRESHOLDS/COMMANDS filled and live. `sources.js` implements the Gen3/Gen4 source selector, verified live in browser 2026-07-08. `AWAITING TELEMETRY` graceful-degradation state added same session. |
| `firmware/` | Done | XIAO ESP32S3 PlatformIO project functional. |

---

## Last session summary

**Date:** 2026-08-09/10 (session — confirmed boot_ts tick rate independently; implemented
the sidelined TST stage-sum estimator for real, then gave it full command authority on the
owner's explicit override of his own validation gate ("Door B"); found and fixed a real
bug the next morning; first low-end validation night landed and tracked against the
owner's own account)

- **Boot_ts tick rate confirmed at 10.0/sec**, independent of the three previously
  disputed candidates (244.1 / 14.279 / ~104), via real unix timestamps embedded in
  `Time sync` (0x42) payloads — 41 anchor points, 08-02 through 08-09, converging to
  9.999946 ticks/sec end-to-end. Re-running Track B condition #1 with the corrected rate
  did **not** close it — transition alignment still ~0.8%, same below-chance signature as
  before. Rules out "wrong tick rate"; the real blocker is now the epoch-anchoring method
  itself. Commit `8b1abf5`. Full detail: `known_issues.md` 2026-08-09.
- **`tst_from_stages.py` implemented** (light+rem+deep sum, robust to stage-subdivision
  error, not claiming independence from the bout-tail estimate — both read the same 0x4C
  struct fields). Wired into `gen3_bridge.py` as `sleep_duration_stage_sum_hrs`/`_meta`
  only; `sleep_duration_hrs` untouched; merge-exclusion added. Commit `3074762`.
- **Door B — owner's explicit, informed override**, same day: wired into
  `engine/sources.js` as a third `sleepDurationHrs` fallback tier behind
  `STAGE_SUM_FALLBACK_ENABLED` (kill switch — `false` + redeploy reverts the tile to a
  dash, no other change needed). This gives the field full command authority: a reading
  under 7h now fires `INITIATE SLEEP PROTOCOL` and feeds `calculateBRI()`. Mitigation:
  `MIN_TIB_MIN` raised 120→420 (7h) to stop a partial/reset bout fragment from reaching
  the tile. Verified end-to-end (a low value really does fire the command, not just
  resolve a number). Commit `5c4f8d3`.
- **Real bug found and fixed the next morning:** the stage-sum field went stale-null on
  any cycle without a fresh 0x4C firing that same 5s window (most of the day), even
  though `sleep_stages` itself sat correctly backfilled right next to it in the same
  JSON — confirmed live before fixing. Fixed by recomputing after
  `merge_with_existing_bridge()`'s backfill, against whatever `sleep_stages` ends up
  being, never backfilled directly. Commit `3e626b1`.
- **First real low-end test, same morning:** the 08-09/10 night structurally differed
  from every night since 07-26 — 2 new 0x4C bouts finalized, only 1 carryover (every
  prior night: 0 new). Stage sum landed at **5.26h (5h16m)**, the first reading under the
  7h warn threshold — `SLEEP PROTOCOL` fired live for real. Independently checked against
  the owner's own account (awake at 22:00 daemon start, asleep after 23:00, woke 05:20,
  ≥2 real bathroom trips correlating with 6 watchdog restarts that night) — 123min of
  classified wake time is consistent with that account, not an obvious fragment. Owner's
  verdict: "5.3 hours sleep is acceptable for now as an ongoing refinement" — first
  tracked point in the 7-morning validation log. Full detail: `known_issues.md`
  2026-08-09/10.
- Daemon stopped early on request (SIGINT to the watchdog PID, not the daemon directly —
  the watchdog's own documented `KeyboardInterrupt` path kills the daemon subprocess
  cleanly without treating it as a stall needing a restart). Confirmed clean via log:
  `[WATCHDOG] Stopped by user -- terminating current daemon subprocess.`

---

**Date:** 2026-08-08 (session — reviewed a proposed second, independent Total-Sleep-Time
estimator from a chat/screenshot upload; found it arithmetically correct but SIDELINED,
not implemented, pending open items and tonight's separately-queued daemon items)

- ⚠️ **Staleness flag:** this file had not been updated since 2026-07-26 despite
  `known_issues.md` showing real, dated entries through 2026-08-08 (Track B condition #5
  closed at 25/25 nights; a WakeDetector quiet-gap investigation on 08-08). Whoever picks
  this up next should treat the 12-day gap (07-27 through 08-07) below as **not
  reconstructed here** — read `known_issues.md` directly for what actually happened in
  that window rather than assuming this file's silence means nothing happened.
- **What was reviewed:** owner pasted (chat only — no file written to the repo) a proposed
  `pipeline/tools/tst_from_stages.py` plus a wiring snippet into `gen3_bridge.py`'s
  `build_bridge_data()`. It sums decoded sleep-stage minutes (wake/light/rem/deep) into a
  TST estimate, hard-gated (TIB/TST bounds, efficiency band, deep-fraction cap, snapshot
  staleness), writing only to a new provisional field `sleep_duration_stage_sum_hrs`
  (+`_meta`) — `sleep_duration_hrs` deliberately left untouched, matching the existing
  0x4C-authoritative rule.
- **Verified by hand, not just read:** all 9 of the author's decline-logic test cases
  check out (observed night: TIB=635min, TST=542min → 9.03h/9h02m, efficiency 0.8535,
  deep 0.1024, matches claimed output exactly). No arithmetic bugs found.
- **Real conflict flagged, unresolved — this is the load-bearing open question:** the
  estimator draws on the sleep-stage decoders (0x61/0x09, 0x5A stage labeling), which the
  `methuselah` skill's data-trust tiering places on **DISCARD — "do not chase, do not
  build."** The code's own docstring says as much. Hard-decline gating and a
  provisional-only field reduce the practical risk of a bad number reaching a tile, but
  don't resolve whether this counts as a legitimate narrow exception (TST cross-check,
  not a stage-% claim) or a re-opening of that closed chase. Owner has not yet decided.
- **Three technical items also flagged, not yet closed in the pasted code:**
  1. `stage_age_min` is used in the wiring snippet but never shown defined in
     `gen3_bridge.py` — if it isn't already real, that's a `NameError` that would crash
     `build_bridge_data()` and blank the *entire* nightly bridge push, not just this field.
  2. Bridge key names (`sleep_wake_min` etc.) are inferred from a UI display string, not
     grepped against actual decoder/bridge source. Low risk if wrong (safe
     `missing_stage_fields` decline, not silently wrong data) but still unverified.
  3. `merge_with_existing_bridge()` has no exclusion yet for the new field — risk of a
     stale backfilled value silently overwriting a correct `None` decline, defeating the
     field's own point.
- **Decision: sidelined, nothing implemented.** No file was added to the repo this
  session — the full source exists only in this chat transcript. Owner chose to hold this
  until after tonight's separately-queued items (owner's own, not detailed in this
  session) are reviewed tomorrow. Revisit gate: (a) owner consciously accepts or rejects
  the DISCARD-tier conflict above, AND (b) the three technical items are closed, AND (c)
  the wiring call is wrapped in try/except so a failure in this path can't take down the
  whole bridge write.
- Full review detail (all 9 test-case checks, threshold-by-threshold reasoning) exists
  only in this session's chat — not reproduced in `known_issues.md`, since nothing was
  actually implemented or tested against real data this session.

---

**Date:** 2026-07-26 (session 6 — investigated + fixed a RECURRENCE of the bridge-overwrite bug: the 2026-07-25 20:33 ACTIVE WINDOW pull nulled fresh RHR/IBI_HR/SPO2/TEMP. Same failure class as the 2026-07-19 overwrite.)

- **What happened (reproduced, not inferred):** the last real bridge write was
  the **2026-07-24 06:14:35** SLEEP WINDOW pull (rhr 55.8 / ibi 55.9 / spo2 92.1
  / temp 33.87 / batt 75.8). The **2026-07-25 20:33** ACTIVE WINDOW pull pushed
  those four as NULL. The assumed "14.3h old" was wrong — the bridge was
  **~38.3h old**, because the launchd daemon has been dead since ~07-24 (EPERM,
  see prior session) so no fresh SLEEP push landed overnight.
- **Root cause:** `merge_with_existing_bridge()` had a hard `age > 18h`
  early-return that abandoned all backfill → the null new-pull vectors overwrote
  real data. The ACTIVE-WINDOW downgrade guard (fires only <18h) also let the
  push through. Both defenses failed *open* on data older than 18h. Nulling real
  data was the destructive move the 18h cap conflated with "don't show stale as
  fresh."
- **Fix (only `pipeline/tools/gen3_bridge.py`):** merge now backfills null
  vectors from the existing bridge **regardless of age** (never nulls real
  data). When a pull carries no biometric of its own (`BIOMETRIC_VECTOR_FIELDS`)
  it inherits the existing bridge's **older timestamp**, so preserved data
  renders STALE (isStale, STALE_HRS=12) instead of masquerading as fresh —
  honors rule #5 without touching tile rendering. `MERGE_MAX_AGE_HOURS` removed
  (its goal is now met by the timestamp downgrade, without data loss).
- **Regression-tested 12/12** (`scratchpad/regression_bridge_merge.py`): ACTIVE
  over fresh sleep (preserved+stale); SLEEP with fresh biometrics over older
  sleep (fresh wins, not aged); >18h existing (preserved but stale, not
  resurrected fresh); daemon recompute (never merges, unaffected).
- **Recoverability:** the 06:14 values are gone from remote+local (KV is a
  single key, overwritten twice); only the raw pull
  `gen3_pull_20260724_061435.txt` retains them.
- **Scope honored:** `classify()`, `sleep_duration_estimate.py`, daemon,
  watchdog, tile rendering untouched (this part of the session).

**Part 2, same session — daemon EPERM root-caused and FIXED, live-verified:**
- **Restored real tile data as an immediate stopgap:** decoded
  `gen3_pull_20260724_061435.txt` via `recompute_bridge_from_daemon.py` (it
  already handles this file format) and pushed it live with its **real**
  2026-07-24 06:14:35 timestamp (not `now()` — corrected manually before
  push), so it renders honestly STALE rather than falsely live.
- **Root cause #1 (file-open EPERM), confirmed via live TCC.db query (not
  inferred):** Full Disk Access has **zero grants on this account, for
  anything** — the 07-23 grant attempt never persisted. Desktop-Folder grants
  are all keyed to real app-bundle identifiers, never a bare binary — and
  `/usr/bin/python3` (what the plist execs) is a bare system shim
  (`com.apple.dt.xcode_select.tool-shim`, no TeamIdentifier). Structurally
  ungrantable on this Mac, not a misconfiguration to retry.
- **Fix #1:** moved the whole repo `~/Desktop/METHUSELAH` → `~/methuselah`
  (out of the TCC-protected Desktop folder). Updated the plist, both
  `pull_*.sh` wrappers, `SHORTCUT_SETUP.md`/`SHORTCUT_SCRIPT.txt` (iOS
  Shortcut app itself still needs manual update on the phone — outside my
  reach), and usage-example paths in five tool scripts. Verified live:
  `launchctl kickstart` got past the file-open crash for the first time ever.
- **Root cause #2 (Bluetooth), hidden behind #1 until now:**
  `BleakError: BLE is not authorized`. Same structural pattern — 
  `kTCCServiceBluetoothAlways` is bundle-only too, confirmed via TCC.db.
- **Fix #2:** plist now spawns the daemon inside Terminal.app via `osascript`
  (mirrors `SHORTCUT_SCRIPT.txt`'s existing working pattern) instead of
  invoking `python3` directly.
- **Verified live, real data, end-to-end:** kickstart → real Terminal window
  opened running the daemon → connected to the real ring → decoded 2 real
  cycles (256 events each) → pushed to the live bridge twice, successfully.
  **First successful launchd-triggered daemon run ever** (prior: `runs=9`,
  all exit code 2). Stopped gracefully via `SIGINT` once verified (daemon's
  own documented Ctrl+C handler) rather than let an unsupervised 8h BLE
  session run mid-day. Job remains loaded for tonight's real 22:00 fire.
- **New edge case surfaced (not caused by today's fixes, pre-existing bridge
  schema gap):** a pull with *some* but not all fresh biometrics (real case:
  fresh `rhr_bpm`/`sleep_temp_c`, preserved 2-day-old `hrv_ms`/`spo2_avg_pct`)
  keeps the whole-payload timestamp fresh, since the bridge has one shared
  timestamp for all vectors — so the preserved fields ride along as
  falsely-live. Only affects mixed fresh/preserved pulls, not the common
  all-preserved case the merge fix targets. Flagged for a future session
  (per-vector timestamps, or accept as known-narrow). Tile rendering/schema
  untouched this session per scope.
- Full detail: known_issues.md 2026-07-26 entries (both parts).

**Part 3, same session — reviewed a live post-fix screenshot, attempted Track
B condition #1, tuned sleep-duration thresholds:**
- **Confirmed the fixes reached production**: live screenshot showed real
  HRV/RHR/glucose values rendering (direct result of parts 1-2 above).
- **Flagged, not fixed (owner said leave as-is for now):** (a) the
  "INITIATE ACTIVE RECOVERY PROTOCOL / EXECUTE PROTOCOL" panel
  (`engine/commands.js` lines 26-28) is pre-existing code that conflicts with
  the project's own non-negotiable rule #2 (no CTA/suggestion layer — see
  methuselah skill); (b) the 85.8 bpm driving that "CARDIAC LOAD ELEVATED"
  warning was the midday active-state value from this session's own daemon
  verification test, not a resting reading — the alert is likely firing on
  mislabeled data on top of the design-rule conflict. Both left untouched
  per owner instruction.
- **Track B condition #1 attempted, blocked on a real, newly-identified
  issue (not data availability):** built
  `pipeline/tools/track_b_condition1_crossref.py` to cross-reference 0x6A
  state transitions against the richest real 0x5A sleep-phase bout on record
  (07-19 daemon night, 1252 valid epochs). Found `boot_ts`-to-wall-clock tick
  rate cannot be reliably derived — three independent methods on the same
  file disagree by up to 17x (244.1 vs. 14.279 [the existing pipeline
  constant, now suspect] vs. ~104 ticks/sec). Running the analysis with any
  one of them produced results that don't survive a sanity check (a
  below-chance state/stage match rate — the signature of misalignment, not
  a real finding). **Condition #1 stays OPEN**, now with a specific,
  actionable blocker (need an independent wall-clock calibration anchor)
  instead of the previous vague "cross-reference 0x6A with 0x5A" note. Full
  detail: known_issues.md 2026-07-26 part 3.
- **Sleep Duration tile — clarified scope, no manual entry needed.** Owner
  asked about inputting sleep duration; turned out they wanted a rough
  ring-derived approximation with Green/Amber/Red banding, not manual entry.
  That mechanism (`sleep_duration_estimate_hrs`) already existed end-to-end
  (tile already renders `~X.X hrs (est.)` when populated) — it's been empty
  because the 0x4C/bedtime cluster it depends on went quiet 07-23 onward,
  correlating with the daemon EPERM outage just fixed in part 2. It fired
  reliably 5 consecutive real nights before that (07-18 through 07-22).
  **Only change made:** `engine/thresholds.js` (+ its build-synced copy
  `web/src/engine/thresholds.js`, via `web/package.json`'s `prestart`/
  `prebuild` scripts) — `sleepDurationWarn`/`sleepDurationCritical` and the
  matching `BRI_BRACKETS.sleepDuration` band changed from 8h/6h to 7h/4h
  (green ≥7h, amber 4-6h, red <4h), per owner's exact spec. Verified: dev
  server compiles clean, every call site confirmed to read from
  `THRESHOLDS`/`BRI_BRACKETS` dynamically (no hardcoded duplicates). **Not
  visually confirmed live** — `sleepDurationHrs` is currently `null` in real
  data, no fake data injected (real-data-only discipline); tonight's daemon
  run is the natural first real test if the bedtime cluster fires again.

---

**Date:** 2026-07-25 (session 5 — asked to build a launchd-scheduled periodic Gen3 pull to auto-populate the 4-vector grid. STOPPED AT THE PREREQUISITE GATE: the existing daemon launchd job still cannot execute; the 2026-07-23 Full Disk Access grant did NOT fix it. No scheduled job built — a job that can't run is worse than none.)

- **Prerequisite (per the task): verify `ca.methuselah.gen3daemon` can actually
  launch now, post-FDA-grant, before building a second job on the same
  foundation. Result: it still fails.** Live reproduction at
  **2026-07-25 11:17:20** via `launchctl kickstart -k` —
  `runs` 7→8, `last exit code = 2`, job died instantly (never started the 8h
  daemon), and `daemon_launchd_err.log` grew 7→8 lines with the same
  `can't open file '.../oura_gen3_ble_daemon.py': [Errno 1] Operation not
  permitted`. The last unattended fire (07-24 22:23) failed identically.
  Both are dated *after* the 07-23 FDA grant → the grant did not fix launchd
  execution. **No launchd run has ever succeeded (`runs=8`, all exit 2).**
- **Root cause isolated by real elimination:** file exists, unix perms
  `-rw-r--r--`; the SAME Xcode python3 opens the SAME file fine from a Terminal
  context (`open OK, bytes=31272`). The only variable that flips
  success→failure is launcher identity (Terminal vs launchd). Script lives in
  `~/Desktop/` (TCC-protected `SystemPolicyDesktopFolder`); the launchd agent's
  context lacks access to that folder, and user-domain `TCC.db` shows no Full
  Disk Access row for python. It's a file-open EPERM — dies before touching
  Bluetooth; distinct from the separate 07-23/24 "ring found zero times"
  Bluetooth question.
- **Not built (deliberately):** the periodic-pull plist, its concurrency guard,
  and its attempt-logging. `merge_with_existing_bridge()` failure-safety
  verification also deferred — all moot until the launchd context can open a
  Desktop file. Nothing in scope was touched (no tile/gate/`classify()`/
  `sleep_duration_estimate.py`/daemon changes; no new pull path). Full detail:
  `known_issues.md` 2026-07-25 "GATE FAILED" entry.
- **Expectation set (unchanged):** fixing the launchd context would restore HRV
  + Cardiac Load freshness. Sleep Duration stays blank regardless — never
  computed; that's the wake-window test, not this.

**Date:** 2026-07-23 (session 4 — investigated the 07-22/23 morning-pull failure; found it recurs on ALL 4 nights, not just the deadlock night; UNRESOLVED, but instrumented for next time — no speculative timing fix shipped)

- **Investigated the 07-22/23 "Ring not found in scan window" morning-pull
  failure as a possibly-separate handoff-timing issue from the confirmed
  `connect()` deadlock (prior session's entry above).** Real finding: this
  is **not** confined to that deadlock night — `pipeline/data/raw_pulls/
  gen3_morning/` and `gen3_evening/` have **zero output files for any of
  the 4 real overnight daemon nights** (07-19/20 through 07-22/23),
  including 07-19/20, the one fully healthy night with zero connectivity
  problems. The automated post-daemon morning pull has never once
  succeeded since it was built (2026-07-12) — a real, general problem, not
  something the deadlock explains away.
- **No exact real timestamps could be recovered for any of the 4 nights**
  (daemon's own POST-RUN section and the morning pull's scan logic only
  ever `print()`, never write to a surviving file — lost once `/tmp/
  daemon_tonight.txt` is gone). Derived what's possible for 07-22/23 from
  real bridge-file mtimes only (recompute+push at `05:16:53`, ~2s after
  that night's nominal end — implying the loop exited naturally, not via
  manual kill as previously assumed, revising that detail from the prior
  session's writeup).
- **The in-session 2-second reconnect gap does not validate the 10s
  cross-process buffer** — checked and found this comparison doesn't
  transfer: in-session reconnects stay within the same process/
  CoreBluetooth session, while the POST-RUN handoff is a genuinely
  different, cross-process peripheral-bond release the code's own
  existing comment already flags as a distinct macOS quirk.
- **No speculative timing change made.** There is no real measurement of
  how long the OS-level bond release actually takes on this machine —
  not from any successful case (none exist) or partial one. Per explicit
  instruction, did not guess a new number for the 10s wait or 120s scan
  timeout; both are unchanged.
- **What was built instead: real-timestamped, persistent handoff logging**
  (`pipeline/logs/morning_pull_handoff.log`, new) in both
  `oura_gen3_ble_daemon.py`'s POST-RUN section and
  `oura_gen3_morning_pull.py`'s own scan attempt — survives independently
  of `/tmp`, regardless of invocation path. Not a fix; sets up the *next*
  occurrence to actually be diagnosable with real elapsed-time data.
  Smoke-tested (mocked scan failure + an incidental real unmocked scan
  attempt in this environment) — confirmed both paths log correctly.
- Full writeup in `known_issues.md`, 2026-07-23 "UNRESOLVED" entry.
- **Next session:** tonight's watchdog test (see item 0 below) will also
  be the first real data point for this issue — check
  `pipeline/logs/morning_pull_handoff.log` in the morning regardless of
  whether the watchdog itself had to intervene. If it shows a real scan
  duration/outcome, that's the first-ever real evidence to size an actual
  fix against instead of guessing.

**Date:** 2026-07-23 (session 3 — built a subprocess-level watchdog for the recurring connect() deadlock; ADDRESSED, not yet RESOLVED — tonight is the first real test)

- **Built `pipeline/tools/gen3_daemon_watchdog.py`.** Detects the deadlock
  via wall-clock time since the log file's last real write (chosen over
  the 2026-07-22/23 incident's garbage-burst tail signature — that
  signature is real but not reliable, since the 2026-07-21/22 likely-
  deadlock shows the identical multi-hour-silence symptom with a
  perfectly clean tail). Threshold `STALE_MINUTES=20`, justified against
  the real 11/74/155-minute gaps already on record (11min = confirmed
  healthy, clears with ~1.8x margin; 74/155min = deadlock-shaped, clear
  with 3.7x–7.75x margin) — with the real tension (scan_for_ring() can
  legitimately run silent up to 30 real minutes in one call) documented,
  not glossed over. On trigger: SIGTERM (10s grace) then SIGKILL the
  daemon subprocess, then relaunch it fresh against the *same* log_path.
- **`oura_gen3_ble_daemon.py` gained resume support** (new
  `_last_real_boot_ts_in_log` + `is_resume` check, both additive — no
  existing behavior changed for a normal no-args launch, confirmed by
  diff): a restarted process appends rather than overwrites, skips
  re-writing the header line so `recompute_bridge_from_daemon.py`'s
  single-header parsing (untouched) still sees one true start time for
  the night, and seeds `last_boot_ts` from the file's own last real entry
  so it doesn't re-fetch (and re-log as duplicates) the ring's whole
  buffer. No second reconnection mechanism was built — every restart is
  just a normal fresh launch of the already-validated 2026-07-21
  scan-then-connect logic in `gen3_ble_connection.py`.
- **Strictly scoped, confirmed by diff before committing:** `classify()`,
  `sleep_duration_estimate.py`, `recompute_bridge_from_daemon.py`, and the
  bridge were not touched.
- **Tested against a simulated hang, not a real overnight run** (per
  task instruction — real hardware wasn't available/appropriate for this):
  pure threshold logic tested directly against the real 11/74/155-min
  gaps; a fake daemon subprocess run under the real watchdog loop
  confirmed zero false-positive restarts on a healthy run, exactly one
  correctly-timed restart on a simulated hang, no data loss/duplication
  across the restart boundary (one header, both segments' events present),
  and SIGKILL escalation against a SIGTERM-ignoring process. **This
  testing caught a real bug, not just confirmed the design clean:** the
  first version waited unboundedly for the *final* segment's own shutdown
  once nominal end time was reached — a SIGTERM-ignoring fake final
  segment hung the watchdog itself with nothing left to catch it. Fixed
  with a bounded `FINAL_SEGMENT_GRACE_SECONDS=600` wait that escalates to
  the same kill path; re-tested and confirmed bounded.
- Full design rationale + real-data threshold justification in
  `known_issues.md`, 2026-07-23 "ADDRESSED" entry.
- **Next session / tonight:** deploy — launch
  `gen3_daemon_watchdog.py` instead of `oura_gen3_ble_daemon.py` directly
  (see next-session-priority item below for the exact command). This is
  the first real overnight test; do not mark the deadlock issue
  "RESOLVED" in known_issues.md until a real night confirms either (a) a
  real deadlock occurred and the watchdog recovered it, or (b) a full
  healthy night completed with zero false-positive restarts.

**Date:** 2026-07-23 (session 2 — regression fix: tick_rate print was 300x wrong; investigation found a real 2nd BLE deadlock incident underneath, not a 2nd computation bug)

- **Fixed a real, reproduced bug in `recompute_bridge_from_daemon.py`: the `Tick rate: ...` diagnostic print (dead code since 2026-07-18, accidentally reactivated by 238da35's header-regex fix) divided the observed boot_ts span by the *nominal* `--duration 8.0h` argument instead of the real, already-computed `session_span_hrs`, compounded by a glitch filter (`<100_000_000`) that let one isolated `UNKNOWN (0x11)` outlier through.** Fixed to use `session_span_hrs` as the denominator and filter by `EVENT_TAGS` membership (mirrors the same fix `sleep_confidence_analysis.py`'s `derive_tick_rate` already made independently 2026-07-22). Confirmed `tick_rate` is never consumed downstream — console-only bug, zero bridge-output impact.
- **The task's other assumption — that `session_span_hrs=5.41h` was also wrong — did NOT hold up under real-data investigation, and nothing was changed there.** `pmset -g log` confirms the Mac stayed awake all night (no sleep/wake events between 21:15:47 and 05:44:23), but the real log's own captured data stops cold at `boot_ts=76192248` followed by `UNKNOWN (0x11)` garbage and then total silence — file mtime frozen at 02:41:25. `oura_gen3_ble_daemon.py`'s own reconnect-loop comment cites an identical prior incident (2026-07-17: "connect() blocked from 04:31 until manually killed at 05:27"). This is very likely a **2nd confirmed occurrence of the known `bleak`/CoreBluetooth `connect()` deadlock**, not a session-span computation bug — `session_span_hrs` correctly detected the early termination, which is exactly what 238da35 built it to do.
- **Verified against real data throughout, not assumed:** reproduced the exact buggy output (5.41h / 3442.924) against the real log first; ran the fixed script against all 5 real logs on disk (07-18/19 through 07-22/23), diffed full before/after output — only the `Tick rate:` line changed anywhere, everything else (bout stages, HRV, RHR, spo2, sleep_duration_estimate_hrs, session_span_hrs) byte-identical. Fixed tick rates (104–270 ticks/sec) land within 20–25x of the 655/min bout-local figure, matching the already-documented "~22x whole-log-vs-bout-local" discrepancy rather than resolving it (expected, not a residual bug). Re-tested the outer-ceiling check's actual firing behavior post-fix (real ceiling, artificially tight 1.0h ceiling, no ceiling) — all three behave identically to 238da35's own original verification; confirmed it never depended on the buggy `tick_rate` value in the first place.
- Full writeup in `known_issues.md`, 2026-07-23 entry.
- **Next session:** see "NEW 2026-07-23" item below — if the `connect()` deadlock recurs a 3rd time, it's worth a real fix (e.g. a watchdog that restarts the daemon subprocess if the log file stops growing), not another one-off manual-kill morning.

**Date:** 2026-07-22 (session 4 — Sleep Duration tile was silently reading a field that almost never populates; wired it to fall back to the estimate)

- **Confirmed a real display bug, not just a theoretical one.** The Sleep Duration tile (`web/src/App.js` ~L607, via `engine/sources.js` `resolveVectors().sleepDurationHrs`) read **only** `sleep_duration_hrs` — the strict 0x4C field that, per design, stays `None` almost every night. `sleep_duration_estimate_hrs` (built by `sleep_duration_estimate.py`, e.g. the 2026-07-19/20 night's real 6.6h result) was computed, written to the bridge JSON, and then never reached the screen. Confirmed against the live `web/public/gen3_latest.json` (both fields `null` that night) and against a reconstructed 6.6h scenario.
- **Fix:** `engine/sources.js` `sleepDurationHrs` resolver now falls back to `sleep_duration_estimate_hrs` when the strict field is `null`, tagging the result `isEstimate: true`. `web/src/App.js` tile shows `~6.6` with unit `hrs (est.)` when it's the fallback value; strict values still render exactly as before (no `~`, no `(est.)`). The 7-day history/log-line builder (`App.js` ~L441) had the same strict-only bug — also fixed, `(EST)` suffix added to the log line when the history point came from the estimate. No stage/confidence/pattern UI added — single number + one-word qualifier only, per KISS.
- **Verified:** `resolveVectors()` tested directly (script, not assumption) against three synthetic bridge shapes — estimate-only night → `{value:6.6, isEstimate:true}`; both-null (declined) night → `null`/`AWAITING DATA` (unchanged, doesn't break); strict-populated night → strict value wins, `isEstimate:false`. `npx react-scripts build` compiles clean. Did not verify the rendered pixels in an actual browser session — static/logic verification only, flagged per real-data-only discipline rule 6.
- Canonical source is `engine/sources.js` (`web/src/engine/sources.js` is a build-time copy per `package.json`'s `prebuild`/`prestart` — edited both, confirmed byte-identical after `cp`).

**Date:** 2026-07-22 (session 3 — built the 2 items the whole-session-reconciliation investigation recommended: outer-ceiling decline check + cross-session bout dedup diagnostics)

- **Built both "build now" items from the earlier 2026-07-22 whole-session-reconciliation investigation (known_issues.md), and nothing else from it** — the multi-bout merge stays discarded (no calibration data yet), daemon start/stop stays rejected as a sleep-boundary substitute.
- **Outer-ceiling decline check.** `sleep_duration_estimate.py`'s `estimate_sleep_duration()` gained an optional `session_span_hrs` param + `CEILING_TOLERANCE=1.1`: if a candidate estimate exceeds the real, connected daemon session's own wall-clock span (× 1.1 tolerance), declines instead of reporting — closes the same category of gap that let the already-fixed 92.0-HRS production bug (2026-07-21) reach the bridge, this time for the 0x4C-based estimate path specifically. `session_span_hrs` comes from real logged data only: the log's own header start timestamp to the log file's real mtime (never assumed, never the nominal `--duration` argument).
- **Found and fixed a real bug en route:** `recompute_bridge_from_daemon.py`'s header-parsing regex required integer `poll=`/`duration=` values but every real log logs them as decimals (`poll=5.0s`) — it has **never actually matched on any real run**, `header` was silently `{}` always. Fixed (float-accepting regex); confirmed against all 4 real logs it now parses correctly. The one place this was previously consumed (a diagnostic tick-rate print, not wired to anything pushed to the bridge) was silently always hitting its fallback branch as a result — fixing it only changes that print's accuracy, no other behavior.
- **Cross-session bout dedup, diagnostic-only as scoped.** New `bout_totals_snapshot()` (public) + `prior_bout_totals` param label each bout new-this-session vs. carryover and enrich decline `reason` text/`confidence` accordingly — confirmed by construction and by the regression run that this never changes whether or what the function declines/estimates. Persistence (`pipeline/data/bridge/bout_checkpoint.json`, gitignored, same category as `gen3_latest.json`) lives in `recompute_bridge_from_daemon.py`, not the pure/testable estimate module.
- **Verified end-to-end against real data, not synthetic cases:** ran the actual `recompute_bridge_from_daemon.py` (no `--push`) against all 4 real logs on disk in real chronological order (07-18/19 seeds the checkpoint, then the 3 nights in scope). **07-19/20's 6.6h is exactly unchanged.** 07-20/21 and 07-21/22 still both decline (no false-positive estimates introduced) but now with honest reasons: 07-20/21 → "4 new bout(s) this session, 17 carryover" (matches the investigation's by-hand finding exactly); 07-21/22 → "0 new bouts this session... the ring's firmware never finalized a fresh sleep summary" (replaces the previously-misleading "final bout has only 1 sample," which read as thin-but-real data when the true state is zero new data at all). Also confirmed the ceiling mechanism actually fires (not just stays silent) by re-running 07-19/20's real entries with a deliberately tight `session_span_hrs=1.0` → correctly declined.
- **Next session:** no outstanding follow-up specific to this fix. The still-open ~22x whole-log-vs-bout-local tick-rate discrepancy is unresolved — `CEILING_TOLERANCE=1.1` is a deliberate buffer against it, not a fix. If a real night ever approaches the ceiling (none has yet — largest real margin is 6.6h against an 8.8h tolerance-adjusted cap), that's the first real case to check the 1.1 multiplier against.

**Date:** 2026-07-22 (session 2 — fixed classify()'s 0x6B step-count blind spot, but falsified the specific evidence that motivated the fix)

- **Fixed the classify() gap flagged in the earlier 2026-07-22 sleep-confidence-instrumentation entry: `classify()` (`oura_gen3_ble_daemon.py`) never checked 0x6B step counts.** It now does — a new `step_count` parameter feeds into the existing `has_activity` check alongside `ACTIVITY_TAGS`/sustained motion, gated by a new `MIN_REAL_STEP_COUNT=80` constant.
- **Real-data digging before writing any code overturned the specific example that motivated the task.** The 39 cycles originally cited as "misclassified SLEEP WINDOW despite nonzero real steps" all have step_count values 48-63 — re-checked directly against 1,151 real 0x6B packets across all three overnight logs (07-19/20, 20/21, 21/22) plus the one real controlled ground-truth walk experiment on record (2026-07-07). **0x6B's step_count is not a literal step count at rest** — it's a wrapping idle counter (70-75% of consecutive packets step by exactly +1, wraps every ~16 counts, never reads 0) that only becomes a genuine per-window step signal during real ambulatory motion (confirmed real walk values: 98-101). There's a clean real gap (64-97) with zero data on either side across everything collected so far. `MIN_REAL_STEP_COUNT=80` sits inside that gap.
- **A second real artifact required comparing against the per-cycle MAX single 0x6B packet value, not the existing cycle-level sum.** Found 44 real cycles across the three nights where 2-3 rest-noise packets (each individually ≤63) landed in one poll cycle and summed to 80+ purely from grouping (e.g. `[45, 14, 63]` sums to 122). `oura_gen3_ble_daemon.py` now tracks a separate `max_step_count` per cycle for the classify() check; the pre-existing `total_steps` sum (bridge-facing) is untouched.
- **Regression-tested for real, not assumed: 0 classify() label changes anywhere.** Ran the actual fixed function against all real cycles in all three overnight logs (2,167 / 2,313 / 554 cycles) and all 45 historical `gen3_morning`/`gen3_evening` pull files (the same dataset the 2026-07-21 hour-gate fix was validated against) — identical output before/after in every case, including the originally-flagged 39. This is the correct outcome, not a failed fix: none of the real data on record contains genuine nighttime ambulatory motion at the 0x6B level. Confirmed the fix itself works via synthetic cases (sleep tag + step_count=80 → MIXED WINDOW; step_count=79 → unchanged SLEEP WINDOW).
- **`sleep_duration_estimate.py`, the bridge, and the 2026-07-21 hour-gate `UNCLEAR` logic are untouched** — confirmed by diff, not just intent. `oura_gen3_morning_pull.py` has its own independent inline classification logic (not a shared `classify()` import) and was correspondingly not touched — task scoped this to `oura_gen3_ble_daemon.py:112` specifically.
- **New, real, out-of-scope anomaly found while regression-testing, flagged not fixed:** `sleep_confidence_analysis.py`'s own weighted confidence-score output (a separate, parallel diagnostic, not `classify()`) no longer reproduces the numbers quoted in the earlier 2026-07-22 entry (mean SLEEP WINDOW confidence now 0.43 vs. documented 0.87; divergence count 215/554 vs. documented 48/554) — reproduced identically with or without this session's `classify()` change, so it's unrelated to this fix. Not investigated further.
- Also updated `sleep_confidence_analysis.py`'s own `classify()` call to pass the new `step_count` parameter, so it keeps replaying the real, current function (its docstring already claims to do this) rather than silently drifting out of sync with the signature it fixed.
- **Next session:** if a real night with genuine nighttime ambulatory motion ever occurs, this is the first real positive case to check `MIN_REAL_STEP_COUNT=80` against — none exists yet. The confidence-score discrepancy above is a real, unresolved anomaly in a separate diagnostic script, worth a look if anyone relies on that script's absolute numbers again. `sleep_confidence_analysis.py`'s own `votes["steps"]` logic (cycle-sum, `==0` test) has the same at-rest-noise-floor problem this session fixed in `classify()` — not fixed here, flagged as a candidate follow-up.

**Date:** 2026-07-21 (session 4 — live bug: production showed "92.0 HRS" sleep duration)

- **Fixed a live production bug: methuselah.ca was showing `SLEEP DEBT — 92.0 HRS`, a physically impossible value.** Confirmed via direct `curl` against `https://methuselah.ca/api/gen3-bridge` that production was serving `sleep_duration_hrs: 92.03` from the daemon's last live per-cycle push (timestamp `05:22:12`), not the correct post-run recompute (`null`, computed 6 minutes later locally but never overwrote production — cause not recoverable, last night's run was manual so its push-outcome stdout was never captured).
- **Root cause (real-data-confirmed, not assumed): `oura_gen3_ble_daemon.py`'s live bridge push assumed each "asleep" 0x6A packet represents ~60 real seconds. It doesn't — real cadence is ~2.9s/packet** (9,617 packets across a real 28,131-second session), an ~20x overcount. **NOT** a cross-night accumulation bug (the accumulator variable correctly reset every fresh daemon process, confirmed via per-night log files) — a wrong-constant bug confined to a single long night. Fixed by removing the live per-cycle `sleep_duration_hrs` computation entirely (now always pushes `None`), matching `recompute_bridge_from_daemon.py`'s already-established 2026-07-18 policy (0x6A unreliable, 0x4C authoritative) — this closes **Finding 1 from the 2026-07-19 entry below**, which had flagged this exact gap as "open, not fixed."
- **Separately, confirmed the tile naming/threshold-direction issue the bug report also flagged is ALREADY RESOLVED in source** (commit `c4d6e11`, 2026-07-18: "SLEEP DEBT → SLEEP DURATION"), just not live — **production has not been redeployed since before 2026-07-18**, independent of today's value bug. A stale `web/src/engine/*.js` (root `engine/` was updated by `5983beb` but the web copy wasn't, in the same commit) turned out not to matter for actual builds — `web/package.json`'s `prebuild`/`prestart` unconditionally re-syncs from root every build, confirmed by running `npm run build` locally. Finished committing the already-pending (uncommitted, found at session start) sync anyway for repo hygiene.
- **Verified via real execution, not static review:** `npm run build` compiles clean, built bundle confirmed to contain `"SLEEP DURATION"` and not `"SLEEP DEBT"`; ran the actual committed engine logic directly via `node --input-type=module` against 5 representative values (92.03/8.4/6.5/4.2/null) — threshold direction, tile color, and command-banner text all agree post-fix. **No browser automation tool was available in this environment — no screenshot was produced**, flagged explicitly rather than claiming full UI verification.
- **Flagged, not implemented:** nothing in the pipeline rejects a physically-implausible `sleep_duration_hrs` before it reaches the dashboard (92.03 still renders as calmly "optimal/green" if it recurred via a different bug) — a plausibility ceiling at the bridge layer is a real option for a future session, not implemented here since it wasn't asked for.
- **Next session:** (1) production needs a redeploy to pick up the already-fixed frontend naming (a code-only change here can't do that); (2) if the owner wants the currently-live-wrong 92.03 in the Vercel KV store corrected before tonight's next daemon run naturally overwrites it, that needs an explicit corrected push (re-run `recompute_bridge_from_daemon.py <log> --push` against last night's log) — deliberately not done automatically this session since it's a live production data mutation.

**Date:** 2026-07-21 (session 3 — built the walk-test keep-warm tool, follows the tick-rate investigation session below)

- **Built `pipeline/tools/walk_test_keepwarm.py`, a standalone manual tool to hold the BLE connection open BEFORE a walk starts.** Targets the root cause already confirmed 2026-07-19: the ring's readable buffer window for step-feature events (0x7E/0x7F) is short (~7-12 min) and rolls over before a fresh connect-auth-setup handshake completes if the connection only opens *after* the walk begins. Reuses `scan_for_ring`/`open_connection`/`request_history` from `gen3_ble_connection.py` — the same event-driven scan-then-connect pattern already validated for overnight reconnects — not reimplemented. Connects, then polls every 5s (daemon's validated default) logging every raw event to `pipeline/data/raw_pulls/gen3_walk/`, reconnecting automatically on a drop, running until Ctrl+C. No decode/classify logic, no daemon/bridge/classifier changes — standalone capture tool only.
- **Dry-run verified against real hardware — connection mechanics only, NOT 0x7E/0x7F.** First bounded attempt hit the existing 25s connect timeout (known real-hardware timing quirk, handled correctly — clean rescan, no hang). Second attempt: connected in 18s, held for 12 cycles (~2 min) with zero disconnects, logged 3,073 real events across 23 tag types, clean digest, clean Ctrl+C shutdown, well-formed log file inspected directly. **Does NOT verify 0x7E/0x7F fires during an actual walk** — that's still completely untested. Full writeup in `known_issues.md` (2026-07-21, "Walk-test BLE keep-warm tool" entry).
- **Too hot for a walk today — real walk test deferred to this evening.** Next session should run an actual walk with this tool active.

- **Investigated the 22x whole-log-vs-bout-local tick-rate discrepancy flagged in the 2026-07-20 `sleep_duration_estimate_hrs` session. `sleep_duration_estimate.py` is UNCHANGED — `TICK_RATE_PER_MIN=655` and the decline logic are untouched, per the task's explicit constraint.** Full writeup in `known_issues.md` (2026-07-21, "Investigation: the 22x..." entry).
- **The previous session's leading (self-described "not confirmed") suspect — large tick jumps at BLE reconnects inflating the whole-log figure — is FALSIFIED.** Computed every one of the 425,748 consecutive-entry boot_ts deltas across the full `gen3_daemon_20260719_212709.txt` log: zero regressions, zero zero-deltas, maximum delta anywhere is 600 ticks. The tick stream is smooth and strictly monotonic for the entire 8-hour session — there is no discontinuity anywhere for reconnects to be blamed on.
- **New check also rules out a sleep-state-dependent clock-domain switch:** entry density (ticks-per-log-entry) inside the final sleep bout is statistically identical to an equally-wide window early in the night (9.49 vs 9.46 ticks/entry, near-identical tag composition) — no detectable rate change correlates with being asleep vs. not.
- **A real but insufficient contributing factor found:** the already-documented "carryover" bout (predating this session, a known pre-existing finding) confirms the daemon's first-cycle `since_boot_ts=0` request can surface pre-session backlog — but it's only ~1.2% of the total tick span, far too small to explain a 22x/~95% gap alone. Also confirmed zero mid-session checkpoint resets occurred, so only the very first cycle could have delivered backlog.
- **Re-deriving all 19 bouts' own internal ratios (not just the final bout) shows the previously-documented "18 tightly-clustered 630.2-722.9 ticks/min" figure understates the real spread** — several substantial-total bouts show ratios of 950-3,075/min, traced to legitimate multi-hour uncounted gaps sitting inside a single bout that never formally reset (same known mechanism as near-zero-total bouts, just occurring mid-bout instead of at the start; checked and ruled out as a `CLUSTER_PAIR_MAX_TICKS` pairing bug — the bout_start bytes are confirmed byte-identical across all 7 of that bout's Bedtime-period packets). Flagged as a real risk: a future night whose final bout contains a similar mid-bout gap could get `TICK_RATE_PER_MIN=655` applied to an atypically-fast local region, understating that night's tail. Not fixed — flagged in `known_issues.md` for whoever next touches the tail-conversion step.
- **No clean match to any pipeline structural constant** (poll_seconds, BLE-reconnect cluster count, 255-event buffer size, bout count, 0x4C firing count) explains the ~19.5x-22.9x ratio — it isn't even a single fixed number across bouts, more consistent with two independently-noisy measured quantities than one conversion-factor bug.
- **Root cause of the 22x gap itself remains open.** The physically-forced argument (a bout self-reporting hundreds of minutes of 30-second-epoch classifications cannot fit inside a shorter real-time span) still holds and is reinforced by this session's two rule-outs — whichever figure is "real," it must be the slower bout-local one for duration purposes, not the whole-log figure. What's unresolved: why the whole-log average is genuinely ~22x faster if not jumps and not a state-dependent clock. Leading remaining candidate, **flagged as a possible real correctness issue but not fixed**: the 30-second-epoch assumption in `_decode_stage_total_min` may not correspond to true 30-real-second epochs at the ring's actual native tick rate. No per-entry wall-clock anchor exists in this log to test that independently (`pipeline/logs/daemon_launchd.log` is empty for this run, per the already-known launchd permission bug). Also noted: the "~6h20m real night" reference in `sleep_duration_estimate.py`'s own docstring has no traceable source elsewhere in the project's docs — it reads as an internal plausibility check, not verified external ground truth.
- **Next session:** if this matters enough to resolve fully, the two productive paths are (a) get a firmware-documented epoch duration/tick rate, or (b) calibrate against a future night with an independently verified real sleep duration (not self-reported from this same pipeline). Neither was attempted this session — out of scope for an investigation-only task.

**Date:** 2026-07-21

- **Fixed SLEEP WINDOW classifier misreading intermittent daytime ring wear as sleep.** Trigger: 2026-07-19 dishes episode (~2:30-4:30pm, ring on/off repeatedly with wet hands) got classified SLEEP WINDOW by a 16:33 manual `oura_gen3_morning_pull.py` spot-check. Root cause confirmed against real data: `SLEEP_TAGS` (0x6A/0x5D/0x6F/0x75) are continuous background-sensor tags, not sleep-exclusive ones — the dishes pull has an identical tag/motion/firmware-sleep_state signature to a real 05:44 sleep-check pull (a ring sitting still on a counter looks the same to both the classifier AND the ring's own onboard algorithm as a ring worn during real sleep). Full writeup in `known_issues.md` (2026-07-21 entry).
- **0x53 (wear event) investigated as the proposed fix mechanism and partially falsified — real data, not assumption.** Added `pipeline/decoders/0x53.py` (format confirmed against real payloads + `open_ring/enums.py`'s STATE_CHANGE enum). Found: 0x53 essentially never survives into a single `morning_pull.py` session (0/29 historical `gen3_morning` pulls ever captured one, including every legitimate sleep-check pull to date — the ring's flash buffer is storage-size-limited and high-frequency tags evict it first). Where it IS available (the daemon's continuous 8h sessions), `NOT_IN_FINGER`/`FINGER_USER_ACTIVE` alternate 33-44x per night even during confirmed real sleep — a wear-confidence oscillation, not literal removal — so naive presence/absence gating on it would have misclassified genuine sleep nights. The one usable signal: `STATE_CHARGING_PHASE` (state 8, confirmed via 3 identical real payloads) is wired in as a hard veto since the ring cannot be worn while charging.
- **Actual fix: `classify()` (in both `oura_gen3_ble_daemon.py` and `oura_gen3_morning_pull.py`) now also downgrades SLEEP WINDOW → UNCLEAR when the pull's own local hour falls outside `PLAUSIBLE_SLEEP_HOURS` (20:00-08:59, chosen from the real daemon schedule) with no activity-tag corroboration, plus the 0x53-charging veto above.** Verified: the 2026-07-19 16:33 dishes pull now correctly classifies UNCLEAR; the 2026-07-19 05:44 real sleep-check pull is unchanged (SLEEP WINDOW); all wall-clock hours spanned by the 3 most recent real overnight daemon runs (07-18/19, 19/20, 20/21) fall entirely inside the plausible window, so the new gate never fires during real sleep — confirmed no regression. Replaying old vs. new classifier across all 45 historical raw pulls shows 14 flip from SLEEP WINDOW to UNCLEAR, all of them daytime (09:12-18:53) — this was a recurring bug, not a one-off; every 22:xx-08:xx historical pull is unchanged.
- **`sleep_duration_hrs`/`sleep_duration_estimate_hrs` untouched per task constraint.** `recompute_bridge_from_daemon.py` (the authoritative post-run bridge writer) hardcodes `pull_class='SLEEP WINDOW'` and never calls `classify()`, so it's entirely unaffected by this fix regardless.
- **Next session:** no outstanding follow-up specific to this fix. If a future daytime nap needs to register as genuine SLEEP WINDOW (rather than UNCLEAR), that's a real product-policy question — not fixable by wear/tag data alone given what's actually observable — worth a deliberate owner decision rather than silently loosening the hour window.

**Date:** 2026-07-20 (session 2 — implementation, follows the investigation-only session below)

- **Built `sleep_duration_estimate_hrs` — a new, PROVISIONAL, separate field from `sleep_duration_hrs`.** Directly implements the "best next step (not implemented)" flagged at the end of the investigation session below (final bout total + uncovered tail). New module `pipeline/tools/sleep_duration_estimate.py`: groups `Sleep summary (2)`/`Bedtime period` firings into bouts by `bout_start`, takes the final bout, and — only if all decline checks pass — returns `(final_bout_total_min + uncovered_tail_min) / 60`. **`sleep_duration_hrs` itself is untouched, still hardcoded `None`** in `recompute_bridge_from_daemon.py`; the new field runs alongside it, wired into `build_bridge_data()` (`gen3_bridge.py`) as `sleep_duration_estimate_hrs` + `sleep_duration_estimate_info` (reason + confidence dict, always present so a decline is inspectable, not silent). Not surfaced on the dashboard — backend/bridge-only per the task's own constraint.
- **Decline logic built and tested FIRST, per the task's explicit build order.** Four conditions, all real-data-tested: (1) final bout <3 samples → decline; (2) non-monotonic stage-total growth within the bout → decline; (3) uncovered tail >90min (`TAIL_CAP_MINUTES`, tunable) → decline; (4) no sustained post-bout activity signal at all → decline. Condition 4 is checked before 3 in code (3 needs a signal to measure against). "Sustained activity signal" = 5+ consecutive `Motion event`/`Real step feature (1)` packets each within 400 ticks of the last — a single occurrence isn't enough, since both tags fire as background telemetry throughout the night too (confirmed: ~13/min even mid-sleep). Verified against 6 synthetic constructed cases (one per condition + a clean control, 6/6 pass) plus 2 real edited copies of last night's actual log (truncated mid-bout → correctly declines on sample count; truncated right at the final complete sample with no trailing activity → correctly declines on no-wake-signal) — not synthetic-only, per the task's own instruction not to skip real-data edge cases.
- **Happy-path run against last night's real log (`gen3_daemon_20260719_212709.txt`) produces `6.6h`** — final bout 359.5min (matches the investigation session's number exactly) + 36.3min uncovered tail (first sustained wake-activity run started at boot_ts=76407968, 24,697 ticks after the last 0x4C sample). In the same ballpark as the investigation session's manual "~42-44min tail" estimate (that one used the log's literal last line as the anchor; this implementation uses the start of the first *sustained* run instead, which lands a bit earlier — a more principled definition of "first clear signal," not a discrepancy).
- **Tick-rate constant (655 ticks/min) reused from the investigation session, NOT re-derived from a naive whole-log calculation — and re-deriving it this session surfaced a real, still-unresolved inconsistency worth flagging.** Computing ticks/min from the log's literal first-to-last boot_ts over the full real 8h daemon session gives ~14,650 ticks/min (~244/sec) — about 22x faster than the 630-660 ticks/min figure the prior session calibrated locally within individual bouts (via the Bedtime period tag). Both are reproducible from real data; they can't both be "the" tick rate. Used the bout-local ~655/min here because it's the one independently cross-validated against a second tag AND the only one that's physically consistent with a bout that self-reports hundreds of minutes of accumulated stage epochs from a bout_start only tens of thousands of ticks in the past. The whole-log ~244/sec figure most likely reflects large tick jumps at BLE reconnects (many happened overnight — see the investigation session's Finding 3) rather than a real elapsed-time measurement, but this is not confirmed. Flagged in `known_issues.md`, not resolved — out of scope for this session, doesn't block the tail calculation since that only ever operates on a short post-bout window in the same local-rate regime the 655/min figure was calibrated against.
- **Constants, all documented inline in `sleep_duration_estimate.py` with where they came from:** `TICK_RATE_PER_MIN=655`, `MIN_BOUT_SAMPLES=3`, `TAIL_CAP_MINUTES=90`, `SUSTAINED_MIN_EVENTS=5`, `SUSTAINED_MAX_GAP_TICKS=400`, `CLUSTER_PAIR_MAX_TICKS=500`. All tunable, none hardcoded without explanation.
- **Next session:** this is one real night of validation (the same night the accumulator behavior itself was discovered on). `sleep_duration_hrs` stays `None` until several more real nights confirm the estimate is trustworthy — do not shortcut that by promoting `sleep_duration_estimate_hrs` to the authoritative field early. The 22x tick-rate discrepancy above is worth a dedicated look if it ever matters for something beyond this narrow post-bout window.

**Date:** 2026-07-20

- **Second consecutive clean overnight run — reconnect logic reliable across two full nights now.** Daemon ran `gen3_daemon_20260719_212709.txt` from 21:27 to ~05:27 (~8h), 447,111 events logged, multiple real BLE disconnects recovered via the scan-then-connect reconnect path with no data loss. This is a separate result from the 0x4C finding below — reconnect *reliability* is good; what those reconnects incidentally revealed about 0x4C's *firing pattern* is a different, unrelated story. `daemon_launchd_err.log` still shows the same stale launchd permission error as 2026-07-19 (unfixed, low priority — tonight's run happened manually again, not via the `ca.methuselah.gen3daemon` job).
- **0x4C RESOLVED as a per-bout accumulator, not a whole-night counter — confirmed via a second tag, not just inferred.** This was the open question left by the 2026-07-19 entry below ("resetting per accumulation bout... flagging as open, not re-closing"). Decoded `[Bedtime period]` (fires 1:1 alongside every 0x4C, previously unparsed 8-byte payload) as `[bout_start_boot_ts, current_boot_ts]` — both u32 LE. Real data: `bout_start` holds constant across every 0x4C firing within the same bout (confirmed directly in two densely-sampled bouts tonight, 7 and 4 consecutive samples each, smooth monotonic stage-total growth in both), and `(current - bout_start) / stage_total_minutes` is a tight, consistent ~650-660 ticks/minute across 18 independent bout observations spanning the whole night — an internal cross-check between two unrelated tags, not circular reasoning. 29 real firings tonight (not 30 as initially assumed going in — corrected against the actual `grep -c` count) group into 19 distinct bouts by unique bout-start value; one predates the daemon's own log start (leftover from a prior completed sleep session still sitting in the ring's register). Full numbers, the ratio table, and the non-overlap check are in `known_issues.md` (2026-07-19/20 entry).
- **New finding, not yet actionable: naively summing bout maxima produces an impossible 64.5h total across a ~8h session.** The 18 same-night bout windows are confirmed non-overlapping and sequential in boot_ts order, so this isn't double-counted overlap — the register is resetting far more often than real awakenings would justify, most likely tied to ring-side firmware/flash bookkeeping rather than literal wake events. **No safe whole-night total is recoverable from simple arithmetic on these 29 snapshots.** `sleep_duration_hrs: None` (the existing `recompute_bridge_from_daemon.py` safeguard) is correctly unmodified — this session was investigation only, per its own constraints, not a fix.
- **Best next step (not implemented):** the final bout (4 samples, smoothest growth, ends at 359.5min/6.0h) is the most complete single estimate but still has a ~42-44min uncovered tail after its last 0x4C sample before the log's true end (a clear wake/activity signal). Weighting toward "last bout total + acknowledge the tail" is the most defensible direction for a future formula — flagged for next session, not built this one.
- **Minor, low-priority:** the automatic post-daemon morning pull failed with `asyncio.TimeoutError` after the existing 10s CoreBluetooth release buffer — first real-world failure of that buffer, after the longest continuous connection (~7h) it's been tested against. No data lost (the recompute step that actually matters had already run and pushed). Noted as a data point for a possibly session-duration-scaled buffer; no code changed.

**Date:** 2026-07-19

- **First full Gen3-only overnight test — reconnect fix VALIDATED, two new findings, one fixed.** Daemon ran 22:24:58→~05:44 on real hardware with multiple real disconnects; the scan-based reconnect logic recovered every time (no 2h-blocks). Note: tonight's console output (cycle/DIGEST/reconnect-reason prints) was not captured to any file — `daemon_launchd.log` has only the startup line, and `daemon_launchd_err.log` shows launchd itself failed with a permission error trying to open the daemon script, so tonight's run almost certainly happened manually in a terminal rather than via the `ca.methuselah.gen3daemon` job. Fix the launchd permission issue before relying on log capture for future audits.
- **Finding 1 (open, not fixed) — 0x4C fired 24 times overnight but `sleep_duration_hrs` was never computed from it live.** Traced all three bridge-write paths: the daemon's live per-cycle push uses **0x6A**, not 0x4C, for `sleep_duration_hrs` (the epoch-based `stage_epochs×30s÷3600` fix is wired only into `oura_gen3_morning_pull.py`, never into the live daemon loop); `recompute_bridge_from_daemon.py` intentionally hardcodes it `None` pending the morning pull (unchanged, by design); the automatic post-daemon morning pull's ~42min buffer had zero 0x4C events, so it also produced `None`. Net: never null-because-broken, null-because-the-epoch-fix-was-never-exercised-against-live-0x4C-data. Independently reconstructed all 24 raw 0x4C payloads from the log: the epoch→hours arithmetic itself is unit-correct (matches the 2026-07-12 0x5A cross-validation), but per-firing totals are **not monotonic** across the night (swing between ~0h and 6–9h) — likely resetting per accumulation bout (tied to the same disruptions the reconnect fix was recovering from), not a whole-night cumulative counter. The last firing (sleep=4.39h, total=5.81h) is plausible but likely understates the true total. Full detail + numbers in `known_issues.md` 2026-07-18/19 entry. **New open question**: 0x4C bout-boundary behavior under multi-disconnect nights — do not reopen the base epoch-arithmetic validation, that part is confirmed sound.
- **Finding 2 (fixed) — bridge overwrite bug: narrow-window pulls nulled fields they had no data for.** The existing SLEEP/ACTIVE downgrade guard in `oura_gen3_morning_pull.py` (added 2026-07-18) only triggers for `pull_class == "ACTIVE WINDOW"`; tonight's narrow post-daemon pull classified as **SLEEP WINDOW** (had 0x6A/0x75/0x6F tags), so the guard never fired, and it pushed a complete bridge nulling `sleep_duration_hrs`/`sleep_stages`/`deep_sleep_pct`. Fixed by adding `merge_with_existing_bridge()` to the shared `gen3_bridge.py` — backfills any `None` vector field from the existing local bridge (bounded to 18h age, matching the existing guard's threshold) while letting genuinely fresh fields win. Wired into `oura_gen3_morning_pull.py` (covers both the manual run and the daemon's automatic post-run call — same code path) and the daemon's live per-cycle push. Deliberately **not** wired into `recompute_bridge_from_daemon.py`'s `sleep_duration_hrs` field — that `None` is intentional (avoiding 0x6A), not a data gap, and merging there would silently resurrect a rejected source. Verified with a simulated bridge (real `sleep_duration_hrs=6.31` preserved through a null-heavy narrow-pull-style push; fresh fields still won; 20h-old bridge correctly not used to backfill) — not just code review.
- Current live bridge (`pipeline/data/bridge/gen3_latest.json`) still shows `sleep_duration_hrs: null` for tonight, since Finding 1 (no live 0x4C wiring) is unresolved — the Finding 2 fix prevents *future* nights from losing already-good data, it doesn't retroactively recover tonight's value.

**Date:** 2026-07-18

- **Night 4 daemon run completed — `asyncio.wait_for` fix confirmed working overnight.** Daemon launched 2026-07-17 22:47, ran until morning. Two CoreBluetooth timeouts occurred at ~03:00 and ~05:01 — both resolved within 25s and resumed scanning immediately. No 2-hour blocks. Full log at `pipeline/data/raw_pulls/gen3_daemon/gen3_daemon_20260717_224745.txt` (8,962 lines, 3 connection windows). Sleep metrics recovered: RHR 59.4 bpm (state=1 filtered, 37 HR samples), SpO2 avg 93.2% (286 samples, 93-98%), sleep temp 35.01°C.
- **pfsm=8 observed for the first time — candidate CHARGING/OFF_BODY_DOCKED state.** Morning pull (ACTIVE WINDOW, not sleep) showed `pfsm_state=8` in debug data alongside battery charging 80%→100%. This state has no prior label in the corpus. Context: ring was on charger. Logged to `pipeline/data/findings/known_issues.md` (2026-07-18 entry) as `pfsm=8: CHARGING/OFF_BODY_DOCKED candidate`. Do not treat as a sleep-state finding — it's a charger/dock signal.
- **0x5A sleep phase cluster fired again — second ever occurrence.** Daemon log contains the full 0x5A cluster (0x76, 0x49, 0x4C, 0x4F, 0x58, 0x5A) from the 2026-07-17 overnight session. This confirms the cluster is not a one-off artifact but recurs on completed sleep sessions. Decoded hypnogram from the daemon pull: LIGHT=318.5min(637ep), REM?=147.5min(295ep), WAKE?=54.5min(109ep), no_data=155ep. Counts cross-validate against 0x4C Sleep Summary (2) u16 pairs (exact match for stages 1 and 2). Stage 0 vs stage 3 disambiguation still open — stage 3 count in summary doesn't cleanly match raw count, consistent with the 0xFF sentinel hypothesis.
- **OFFLINE badge fixed → three-state OURA LIVE / OURA / OFFLINE.** `web/src/App.js` badge logic changed from 2-state to 3-state: OURA LIVE (blue, fresh <12h), OURA (amber, stale data present), OFFLINE (dim, no data at all). Committed `4c2834e`. `web/src/setupProxy.js` created (committed `c411ad1`) so local dev correctly serves `web/public/gen3_latest.json` via the `/api/gen3-bridge` route that otherwise 404s under CRA's dev server.
- **2026-07-08/09 comparison CSV row logged (corrected values).** Prior claimed values (hr_avg 58.4, spo2_avg 93.8) were wrong. Row inserted at line 11 of `gen3_vs_gen4_comparison.csv` with real decoded values: HR avg 60.1bpm (10 samples, all state=1), SpO2 avg 96.3% (286 samples), sleep temp 35.07-35.20°C. Gen4 fields n/a (API expired). Committed `002301a`.
- **0x5A decoder built and wired.** `pipeline/decoders/0x5a.py` created (committed `002301a`): 23-chunk reassembly, 2-bit LSB-first decoding, 0xFF sentinel exclusion, stage counts/durations, `decode()` / `decode_from_raw_packets()` / `summarize()`. Registered in `pipeline/decoders/__init__.py`. Wired into `pipeline/tools/oura_gen3_morning_pull.py` with a `=== SLEEP PHASE DATA DECODE (0x5A) ===` section.
- **Bridge push 401 resolved.** The `GEN3_BRIDGE_WRITE_SECRET` in `~/.zshrc` (`8c3f5509...`) doesn't match Vercel. The working secret lives in `~/.bash_profile` (last line, `30157c93...`) — that's what the daemon used all night. Pushed sleep metrics manually with the correct secret; got HTTP 200. Live site (methuselah.ca) now shows rhr_bpm=59.4, spo2_avg_pct=93.2, sleep_temp_c=35.01, classifier=SLEEP WINDOW.
- **⚠️ `.zshrc` has a stale/wrong `GEN3_BRIDGE_WRITE_SECRET`.** Clean it up: delete the `~/.zshrc` export entirely or update it to match `~/.bash_profile`'s last line. Until fixed, any push from a zsh shell will 401.

**Date:** 2026-07-17

- **Night 3 results — 6h45m gap, one missed connection window.** Daemon launched cleanly, ran 5 cycles before first disconnect. At 04:31, scanner detected the ring during a TRANSITIONAL sleep-stage window and called `open_connection()`. Ring returned to SLEEP_REGIME before the handshake completed — `BleakClient.connect()` blocked indefinitely. Daemon log shows no further entries until 06:27 when morning pull was manually fired. 6h45m window of missed data.
- **Root cause confirmed from morning pull debug data.** `advertising=0` when `pfsm_state=6` (SLEEP_REGIME); `advertising=2034744` when `pfsm_state=5` (TRANSITIONAL). Ring completely suppresses BLE advertising during deep sleep, only advertises during brief stage transitions. The scan-then-connect pattern (added 2026-07-16) correctly detected the ring during one such transition, but `open_connection()` had no asyncio-level timeout — if the ring stops advertising mid-handshake, CoreBluetooth blocks indefinitely.
- **Fix applied — `asyncio.wait_for` timeout around `open_connection()`.** `pipeline/tools/oura_gen3_ble_daemon.py` reconnect path now wraps `open_connection()` with `asyncio.wait_for(..., timeout=25)`. On `asyncio.TimeoutError`, logs "Connect timed out (ring likely stopped advertising) — will rescan." with no sleep delay (next transition could be seconds away). 25s gives 2.5× the typical 8-10s handshake time while still aborting before the ring can complete a full transition cycle. Full writeup in `known_issues.md` 2026-07-17 entry.
- **Morning pull data recovered.** Ring pulled at 06:27: HR mean 59.8bpm, HRV 51.1ms RMSSD, SpO2 ~92-93%, sleep temp 35.45-35.90°C. 9 × 0x6A samples (4× state=0, 5× state=1). Sleep data for 2026-07-16 night recovered via safety net — not lost.
- **All prior nights' fixes (scan-first, GATT settle, lid-close crash recovery) remain intact.** No regressions.

**Date:** 2026-07-16

- **Night 2 data loss — daemon process died on lid close at 20:26.** Sequence: WiFi dropped (lid close) → bridge push failed → macOS Bluetooth state reset → GATT service table invalidated → `Service Discovery has not been performed yet` on next poll → daemon correctly entered rescan loop → BleakScanner crashed on same Bluetooth reset → unhandled exception killed process. Morning pull safety net fired at 05:31 but user was already up and moving; buffer had rolled over to active-window data. Sleep data for 2026-07-15 night is gone.
- **Three fixes committed:** (1) `scan_for_ring()` now retries on scanner crash (30s windows, try/except, 10s wait) instead of propagating. (2) `open_connection()` adds 1s post-connect pause to let GATT settle — fixes "Service Discovery has not been performed yet" on fast reconnect. (3) `oura_gen3_morning_pull.py` updated to use `scan_for_ring()` — was failing with TimeoutError on direct connect (same macOS bonded-peripheral bug as the daemon).
- **Correct overnight launch command going forward:** `nohup python3 pipeline/tools/oura_gen3_ble_daemon.py > /tmp/daemon_tonight.txt 2>&1 &` — check startup with `tail -f /tmp/daemon_tonight.txt`, then close the lid once connected. Review in morning with `cat /tmp/daemon_tonight.txt`.

**Date:** 2026-07-15

- **Gen4 ground truth permanently closed.** Oura API token expired 2026-07-13. Gen4 app membership inactive. All Gen4 detail views paywalled. **There is no comparison source available now or going forward.** Gen3 BLE data must be trusted and debugged entirely on its own terms. Do not suggest or rely on Oura-app comparison in any future session — this is a hard closed door.

- **Task 1 RESOLVED — root cause of overnight reconnect failure identified.** The daemon was NOT retrying every 10 seconds as designed. `BleakClient(ADDR, timeout=30).connect()` on macOS (bleak 0.21.1 / CoreBluetooth backend) does not respect the `timeout=` parameter for previously-bonded peripherals. CoreBluetooth's `connectPeripheral:` queues the request indefinitely, blocking `open_connection()` for the full ~2 hours between each real ring advertisement window. The `asyncio.sleep(10)` retry code was never reached because the function never returned. Only 5 genuine reconnect attempts occurred all night (21:13, 23:11, 01:12, 03:12, 05:14 — ~2h apart) rather than the hundreds that should have. Log file confirmed: only 2 boot_ts clusters in 5377 lines, no intermediate reconnect-attempt print lines.

- **Task 2 DONE — scan-then-connect reconnect pattern implemented.** Replaced the blocking `open_connection()` retry loop with `scan_for_ring()` (new function in `gen3_ble_connection.py`). Uses `BleakScanner.discover()` in short-window polling loops to detect the ring advertising before calling `open_connection()` — event-driven, not polled-indefinitely. Now reacts within seconds of the ring advertising again rather than waiting for CoreBluetooth's internal timeout to resolve. `scan_for_ring(timeout_seconds, poll_interval)` added to `gen3_ble_connection.py`; daemon reconnect path updated.

- **Task 3 — off-finger vs out-of-range: UNKNOWN, no data.** `known_issues.md` has no documented difference in BLE advertising frequency between ring-on-finger and ring-on-nightstand. `0x53` wear events confirm the ring has wear-state detection hardware, but no finding links wear state to advertising interval changes. `0x61/0x0d` `ticks_advertising_mode` is decoded but never correlated with wear state in the corpus. **Verdict: cannot advise on ring-on vs ring-off during bathroom trips. Conservative default is to keep it on (avoids triggering a wear-state transition event), but this is a hypothesis, not a confirmed finding.**

- **Task 4 DONE — morning-pull safety net added.** At the end of every daemon session, if `sleep_secs_accumulated < morning_pull_threshold_hrs * 3600` (default 4h), automatically fires `oura_gen3_morning_pull.py` as a subprocess to capture whatever the ring's buffer still holds. Independent of Tasks 1-3 — runs regardless of whether the daemon had connection issues.

**Date:** 2026-07-14

- **Tile rework — all 4 tiles now show threshold + 7d avg + trend + staleness inline.** Replaced the progress-bar + single-word status (OPTIMAL/SUPPRESSED) with a 3-line format: label / value + `(optimal X · 7d avg Y · stable|trending up|trending down)` / `● SOURCE · Xmin old [flag: stale]`. No bar element, no composite score, no CTA layer — transparency-as-moat design (show the raw signal, not an editorial summary). Stable zone is ±5% relative to suppress noisy flapping. Staleness threshold 12h: dims tile to 0.65 opacity and shifts source dot to amber. 7-day rolling averages backed by localStorage (lazy-initializer, survives reload). Threshold text always pulled from `THRESHOLDS.*` — never hardcoded in the tile render. Fixed a `metaParts` spread bug during implementation: initial signature was `(threshold, trend)` called with spread that silently dropped the actual trend arg; fixed to explicit `(threshold, avg, trend)` with 3 named params.
- **Repair Depth tile → Sleep Debt.** Renamed from "REPAIR DEPTH" → "SLEEP LOAD" → "SLEEP DEBT" (same session). Underlying vector swap: `deepSleepPct` → `sleepDurationHrs` across all 5 files (`engine/thresholds.js`, `engine/commands.js`, `engine/index.js`, `engine/sources.js`, `web/src/App.js` and their web copies). Gen4 source: `total_sleep_duration / 3600` from Oura API. Gen3 source: daemon 0x6A asleep-packet accumulation overnight. `deep_sleep_pct` infrastructure left intact (pipeline/tools). Command: `SLEEP PROTOCOL · INITIATE SLEEP PROTOCOL TONIGHT.`; rat: `SLEEP DEBT DEFICIENT (X.XH LAST NIGHT).`; threshold 7h floor; BRI brackets `{ optimal: 8, nominal: 7 }`.
- **HRV sleep-window restriction wired in daemon.** `hrv_for_bridge = accum["hrv_ms"] if pull_class == "SLEEP WINDOW" else None` — daytime RMSSD (56-70ms range) is incomparable to overnight (24-31ms); only overnight values pushed to bridge.
- **`web/src/engine/sources.js` HRV fallback fixed.** Web copy had drifted from a prior session — still had `hrv: resolveVector(gen4Fresh?.hrv ?? null, null, null)` instead of the correct Gen3 fallback. Fixed to match `engine/sources.js`.
- **Tile rework verified in browser (no-data state).** Dev server compiled clean. Snapshot confirms all 4 tiles render correctly with correct labels, units, and "AWAITING DATA" source lines (expected — Gen4 token expired, no daemon running).

**Date:** 2026-07-13

- **⚠️ TODAY IS THE GEN4 TOKEN EXPIRY DAY.** Morning pull run and logged as very likely the last comparison-CSV row this project will ever get with real, independently-obtained Gen4 ground truth. Real pull (`gen3_pull_20260713_054157.txt`), zero decode failures, SLEEP WINDOW, 100% state=1. Investigated a large Gen3/Gen4 HR gap (73.6bpm vs 63bpm, ~10bpm, far bigger than any prior night) before logging it as a finding — resolved as a buffer-window artifact, not an error: the `0x6A` sample span was only ~10-12 minutes of real time (buffer only ever holds a slice, not a whole night), and this particular slice landed right before waking on a night Gen4 itself flagged as broken/light-sleep-heavy — an elevated pre-waking HR is physiologically plausible, not a red flag. Also resolved an open thread from last night: the SpO2 bias reversal seen on the prior two nights (Gen3 reading higher than Gen4) reverted back to the historical norm (Gen4 higher) this night — confirms that was a temporary 2-night anomaly, not a permanent shift. Confirmed the `0x76`/Sleep-summary/`0x5A` cluster from last night's 21:20 evening pull did NOT recur this morning — one data point toward "genuinely rare," not "now permanently unlocked." `0x61/0x15` finger detection fired again on its already-characterized ~36000-tick schedule. Full writeup in `known_issues.md`; row appended to `gen3_vs_gen4_comparison.csv`.

**Date:** 2026-07-12

- **BLE streaming daemon BUILT — owner decision resolved, then implemented same session.** Confirmed with the owner that the official Oura app's BLE connection to the ring is no longer worth protecting (data-pipe-only, and that pipe dies with the Gen4 token tomorrow regardless) — the June rejection reason no longer applies. Investigated the four other untouched `pipeline/tools/oura_gen3_*.py` scripts before designing anything: `oura_gen3_daily_pull.py` confirmed the ring's history-request command supports an arbitrary starting `boot_ts`, not just 0 — the key primitive that makes efficient long-poll design possible without needing to test/depend on unverified ring live-push behavior. `oura_gen3_auto_loop.py` was the closest precedent but reconnects/re-authenticates every cycle and always requests the full buffer from 0. Built via plan mode (approved): **`pipeline/tools/oura_gen3_ble_daemon.py`** (new) holds one persistent connection open, polls every 60s (default, CLI-overridable) using the incremental request instead of reconnecting, decodes each cycle's new events through the same `pipeline/decoders/*` used everywhere else, appends everything to a running readable log file (`pipeline/data/raw_pulls/gen3_daemon/`), pushes bridge JSON updates when new priority data arrives, prints a digest every ~10 minutes, and reconnects with ring-reboot detection (boot_ts regression check) on disconnect. Extracted two shared modules to avoid a 6th copy of the auth handshake and a 2nd copy of the bridge-JSON/KV-push logic: `pipeline/tools/gen3_ble_connection.py` and `pipeline/tools/gen3_bridge.py` — the latter is now also imported by `oura_gen3_morning_pull.py` itself (refactored, not rewritten; same behavior, no duplication). Verified without the physical ring first: all four files `py_compile` clean; dry-ran the daemon's full decode→accumulate→bridge-build pipeline against a real archived pull file and confirmed zero decode failures and an `rhr_bpm` matching the live site's known-correct value from earlier in the session.

**Then validated live with the real ring — found and fixed two real protocol bugs in the process, both only discoverable with actual hardware.** (1) Tag `0x11` turned out to be an end-of-transfer terminator/ack, not a real event, with garbage boot_ts bytes that corrupted the incremental-poll checkpoint and falsely triggered the reboot-detection safeguard. (2) A second terminator tag, `0x1f` (empty payload), had the same corrupting effect with different garbage values — fixed generally this time (exclude any tag outside `EVENT_TAGS`' known range from the checkpoint, rather than hardcoding each discovery) instead of patching tag-by-tag. Also fixed a boundary duplicate (the ring's `since_boot_ts` filter is inclusive, not exclusive). **The poll-interval assumption was also wrong** — the whole design was built around "~1.8 min worst case while walking," but live testing found the 256-event buffer filling in ~66 seconds just sitting at a desk, and a measured sustained rate of ~14 events/sec that could fill it in ~18s during active use; default tightened from the originally-planned 60s down to 5s across two real-data-driven revisions. Final validation run: 9,633 real events captured with zero gaps anywhere (including cleanly through a genuine mid-session BLE disconnect/reconnect while walking), and — notably — **255 samples each of `0x7E`/`0x7F` step-feature data**, a tag pair that every prior one-shot walk experiment in this project failed to capture cleanly. Full writeup with all the specific numbers in `known_issues.md`.
- **⚠️ Hours after the roadmap prune below, 0x76/0x49/0x4C/0x4F/0x58/0x5A ALL fired for the first time ever — the "CONFIRMED NON-FIRING" categorization was wrong within the same day.** A 21:20 evening pull run from a separate Claude session caught six tags firing together as one cluster, all previously either "never observed" or "Gen3 does NOT emit": `0x76` bedtime_period (decoded via the existing decoder: a real 362,153-tick sleep span), `0x49`/`0x4C`/`0x4F`/`0x58` Sleep summary (1)-(4), and `0x5A` Sleep phase data — the last one wasn't even tracked in the roadmap before today. Verified the pasted output against the actual saved file before trusting anything (per real-data-only discipline) — also caught and explained a red herring: the pull's own "~36,598 hour span" summary line is the same `0x11` terminator-packet artifact fixed in the daemon this morning, just not yet ported to this one-shot script (cosmetic only, real span is 8,104 ticks). **Real structural win**: `0x5A` is a clean 23-packet chunked transmission (sequential index bytes 0-22, zero gaps) reassembling into a 299-byte structure — the strongest lead yet on the long-standing `sleep_state`-flatness question, though the content itself (tested a 2-bit-per-epoch stage hypothesis) isn't decoded. **Tested, not confirmed**: `Sleep summary (4)`'s 7 bytes `[98,66,100,86,70,58,33]` look suspiciously like Gen4's own 7-field contributor-score shape, checked directly against real July 11/12 Gen4 scores — one exact match per night plus some near-misses, not a clean confirmation, left as an open hypothesis rather than claimed. Also surfaced a secondary finding while trying to place this in time: boot_ts tick rate varies hugely by activity level (~3/sec calm vs ~14-24/sec active today), undermining the project's long-assumed fixed "~3.70 ticks/sec" conversion — flagged, not resolved. `open_ring_roadmap.md` updated throughout (six tag statuses corrected, IN PROGRESS count 3→9, CONFIRMED NON-FIRING section removed as no longer applicable). Full writeup in `known_issues.md`. **Lesson restated in the roadmap itself**: even same-day "confirmed non-firing" isn't permanent — re-verify before trusting any "never happens" claim in this file.
- **Roadmap pruned and closed out — 16 tags moved to CLOSED, 0x76 to CONFIRMED NON-FIRING, and 0x61/0x15 pushed from too-sparse to PARTIAL using fresh daemon data.** Owner requested a prune of `open_ring_roadmap.md` to separate genuinely open work from dead weight. Moved 14 diagnostic-only Tier-3 debug sub-types (zero biometric value even if solved) plus `0x5B` (BLE connectivity metadata) and `0x82`/`0x83` into a new CLOSED section — findings preserved per the roadmap's own rule, just no longer framed as pending. `0x76` moved to a new CONFIRMED NON-FIRING section (real value if it ever fires, but no pending action since there's no known trigger). Separately, `0x61/0x15 _dd_finger_detection` — previously stuck at n=4, "too sparse to hypothesis-test" — jumped to n=11 unique real samples from today's daemon session alone, enough for real analysis: confirmed it fires on a strict ~30-minute periodic schedule (not on wear-transition events as the name implies), and found a clear byte-level entropy split (some bytes near-constant/slow-counters, most still high-entropy/unresolved). Promoted to PARTIAL. **Everything else remaining on the roadmap is now confirmed to be at an external ceiling (firmware/datasheet), not idle from lack of analysis** — the one genuinely actionable item left is the `0x7E`/`0x7F` pace-sensitivity walk comparison, which needs a second walk at a different pace to run (owner action, not something I can do alone). Full writeup in `known_issues.md`.
- **0x61/0x28 and 0x61/0x33 pushed further within PARTIAL — real correlations found, neither promoted to DONE.** 0x61/0x28 (afe_statistics): `stats_hex` confirmed as exactly 6x u16 LE fields across the full 140-record corpus; continuation-record field-shape correlates strongly with the independently-decoded `pfsm_state` (97%/89%/92% match across three distinct shapes — active vs sleep/transitional runs a different AFE measurement scheme); header-record field[0], normally 0, confirmed as a genuine per-channel drift-flag via exact cross-record arithmetic in all 3 nonzero cases. 0x61/0x33 (ppg_settings): the 12-byte settings blob confirmed to split into two structurally-identical 6-byte per-channel (A/B) halves, consistent with the ring's established dual-PPG-channel pattern — channel marker byte, constant register-address byte, and near-fixed calibration offset all identified. Both decoders updated to expose the new structure, re-verified against the full corpus (140/140, 88/88, zero failures), pull script print lines updated to match. Ceiling for both: field/register IDENTITY still needs firmware or a MAX86171 datasheet — this is real structural progress, not a promotion to DONE. Full writeup in `known_issues.md`; `open_ring_roadmap.md` updated.
- **Two comparison-CSV rows logged (2026-07-10/11, 2026-07-11/12) — data delivered via a separate claude.ai conversation, independently re-verified before logging, one real finding surfaced.** Per this project's real-data-only discipline, re-decoded both raw pull files directly rather than trusting the delivered numbers. Nearly everything matched exactly; one minor unresolved discrepancy (0x6E sample count/HR-mean off by a small margin on night 1, not concerning). **New finding: SpO2 bias direction reversed on both nights** — every prior CSV row shows Gen4 reading ~2-5% higher than Gen3 (the basis of the SPO2_OFFSET=6 calibration work); both these nights show the opposite, Gen3 higher than Gen4. Two consecutive nights is enough to flag as a real pattern shift, not noise — worth watching over the next few nights. Also flagged (not concluded): a 0x75 sleep-temp reading notably higher than the historical range on night 1, and a sleep-state distribution anomaly on night 2 that's confounded by that pull's own buffer-rollover warning. Both nights confirmed to have zero pfsm=6 samples despite full Gen4 ground truth — logged explicitly as a gap, not silently omitted. Full writeup in `known_issues.md`.
- **PRIORITY 1 CLOSED — Gen3 fallback confirmed working end-to-end on the live site, verified in a real browser.** Completed the 3-step Vercel provisioning from the 2026-07-11 entry below (Upstash for Redis, Free tier, linked to the `methuselah` project; env vars matched `api/gen3-bridge.js`'s assumptions exactly, no code changes needed) with the owner walking through the Vercel dashboard interactively. `GEN3_BRIDGE_WRITE_SECRET` was rotated twice during setup after accidental exposure (once pasted into this chat, once garbled by a terminal paste) — both treated as compromised and regenerated rather than reused. **Key lesson**: a Vercel env var edit does not affect an already-running deployment; a fresh Redeploy is required, which is what caused two rounds of unexplained 401s before this was caught. Verified for real, not mocked: direct `curl` POST/GET against `/api/gen3-bridge` round-tripped correctly (`200 {"ok":true}`, then read back the same payload); a real ring pull (256 packets, sleep window) logged `[BRIDGE PUSH] 200 → live site updated.`; and the full `SOURCE_SELECTOR_TEST.md` Test A was re-run live against methuselah.ca via Chrome browser automation — with the Gen4 token removed, CARDIAC LOAD correctly showed `63.3 BPM` `ELEVATED` `● GEN3 BLE` (matching the real pull), and the command panel fired `INITIATE ACTIVE RECOVERY PROTOCOL` off that Gen3-sourced value, confirming the command engine reads the resolved vector end-to-end, not just the tile label. Full writeup in `known_issues.md`. **The July 13th Gen4 token expiry no longer breaks the live site.**

**Date:** 2026-07-11

- **Gen3 fallback fix implemented (Option 2, private KV) — code done, one manual Vercel step still needed before it's live.** Checked repo visibility before deciding between the three options from the CRITICAL finding below: **the repo is public**, which ruled out Option 1 (committing biometric data) much harder than the original framing — that would have meant permanently publishing real RHR/SpO2/temp/battery data to the open internet, not just a principle violation. Built: `api/gen3-bridge.js` (new, zero-dependency, matches `api/oura.js`'s style — GET/POST against a Vercel KV store's REST API instead of adding an SDK, since `vercel.json`'s `installCommand` only installs `web/`'s dependencies, not root's, so a new root npm package was a real risk of silently not installing); `oura_gen3_morning_pull.py` now best-effort POSTs the bridge JSON to the live endpoint after writing the local file (never fails the pull over a push error); `App.js` fetches `/api/gen3-bridge` instead of the static file. Verified everything checkable without live infrastructure: compiles clean, `npm run build` passes, and all 8 real code paths in the new route exercised with mocked `fetch`/`req`/`res` (missing config, KV hit, KV miss, missing/wrong/correct write secret, bad payload, unsupported method) — all correct. **Not verified**: the actual Upstash REST wire format against a real store, since none exists yet. **Safe to deploy now regardless** — the existing `.catch(() => null)` in `App.js` means an unconfigured endpoint just degrades to the current `AWAITING DATA` state, no regression. **Owner still needs to**: (1) create a KV/Upstash-compatible store in the Vercel dashboard and link it to the project (auto-injects the read/write env vars), (2) set a `GEN3_BRIDGE_WRITE_SECRET` value in Vercel's project env vars, (3) set the same value wherever the pull script runs locally. Full details in `known_issues.md`.
- **⚠️ CRITICAL — live-site browser verification finally ran (Chrome reconnected), and it found a real, urgent problem: Gen3 fallback does NOT work in production.** Full end-to-end test on methuselah.ca itself: Gen4 read confirmed (`● OURA LIVE`, real matching data). Token removed + reload → CARDIAC LOAD showed `AWAITING DATA`, not `● GEN3 BLE` — the fallback failed. Root cause confirmed via curl: `web/public/gen3_latest.json` has never actually been deployed (Vercel serves the SPA fallback instead) because its source, `pipeline/data/bridge/gen3_latest.json`, is deliberately `.gitignore`d for data sovereignty, so the prebuild copy step silently no-ops on Vercel. Token restored + reload → Gen4 reconnection confirmed working. **This means RHR and SpO2 will go to `AWAITING DATA` after the token expires 2026-07-13, not fall back to Gen3 as the whole source-selector project was built to do** — the code is correct, there's just no data reaching production. This is a real architectural trade-off (data sovereignty vs. a working fallback), not a quick fix — three options laid out in `known_issues.md`, none implemented, needs a decision now given the 2-day window. This is now the single most urgent item, superseding the prior "still needs browser verification" priority (which is now resolved, just with a bad answer).
- **8 newly-decoded Tier 3 tags wired into the live pull script.** Follow-up to the backlog sweep below — `0x61/0x04/0x0A/0x0C/0x0D/0x15/0x28/0x29/0x33` added to `oura_gen3_morning_pull.py`'s existing `0x61` dispatch loop. A future live pull will print decoded sleep stats, both battery sources, flash usage, BLE usage, period info, finger detection, AFE stats, ACM config, and PPG chip ID all in the same debug-data section — not just tag-name-and-hex, actual values. Verified against the richest real debug-data pull in the corpus (76 records, zero failures); real BLE hardware isn't available in this environment, so the next actual pull is the true end-to-end confirmation.
- **Priority pivot: "full access to Gen3 pulls + log everything readable" is now the primary goal, driven by Gen4 expiring 2026-07-13.** Investigated the pull-completeness question first: the ring has a hardware **255-event circular flash buffer** (already fully characterized 2026-06-28, see known_issues.md) — the pull script already requests the maximum possible; there is no way to get more per connection, only to pull more promptly (safe indefinitely while lying still, buffer fully replaced within ~1.8 min of walking). **One decision worth revisiting given the new priority**: a persistent BLE streaming daemon (capture continuously instead of one 255-event snapshot per connection) was explicitly rejected in June because it would break the official Oura app's BLE connection — that trade-off assumed Gen4/the app still mattered. With Gen4 access ending in 2 days, this blocker may no longer apply; flagged as an open decision, not built. Also identified `Cracked-Oura-main` (`~/Desktop/Cracked-Oura-main`) as a resource: NOT a BLE/firmware tool — it is a legitimate third-party desktop app that imports Oura's official GDPR data-export via account email+password login (Playwright automation), separate from the personal access token, so it is NOT bound by the July 13th expiry. Has not been run/populated yet; running it means entering real Oura account credentials into an "Alpha" status third-party tool, a call for the owner, not done by this session.
- **Tier 2/3 decoder backlog swept — 6 tags promoted to DONE, 2 PARTIAL, 19 confirmed dead (not just unattempted).** Surveyed all ~29 untouched `0x61` debug sub-types against the full corpus before decoding anything — 9 had real packets, 19 had zero across all 29 files. Decoded and validated all 9 with real data (406 packets total, zero decode failures): `0x61/0x04` (literal ASCII debug text), `0x61/0x0A`/`0x0C`/`0x0D` (a fixed trio that fires immediately after every `0x09` sleep-statistics event — strong cross-tag validation, two independently-decoded fields from different tags agreeing within single digits across 57 samples), `0x61/0x24` (decoder already existed and was correct — the roadmap claiming "never caught a packet" was simply stale, already decoded twice this week), `0x61/0x29` (clean discrete accelerometer config states). Two PARTIAL: `0x61/0x28` (114 packets, 91% non-zero — richer than open_ring's own reference capture) and `0x61/0x33`, which **confirms the Gen3 ring's PPG sensor chip is a MAX86171** — real hardware identification. `0x61/0x15` checked but too sparse (n=4) to interpret, left raw. Not yet wired into the live pull script's console output — decoders exist and are registered, but this is a distinct next step. Full writeup in `known_issues.md`; roadmap updated (DONE count 10→16).
- **Live-site Gen4 verification — server-side path CONFIRMED working, client-side/browser still BLOCKED, gap NOT fully closed.** Chrome extension still unreachable (4 more attempts this session, 8 total across two sessions — no longer plausibly transient, needs separate attention). Instead of skipping, ran direct HTTP tests against the live Vercel deployment: `curl https://methuselah.ca/api/oura?token=...` with the real current token returned **HTTP 200, `x-vercel-cache: MISS`** (fresh, not cached) with real Oura sleep data for 2026-07-10 — cross-checked against the `gen3_vs_gen4_comparison.csv` row logged the day before, independently: `lowest_heart_rate: 54`, `average_hrv: 28` matched exactly, `average_heart_rate: 59.375` rounds to the recorded 59. Then simulated the actual July 13th condition (step 6): an intentionally invalid token against the same live endpoint returned **HTTP 401**, `{"error":"Oura API error: 401"}` — confirmed by re-reading `api/oura.js` that this is exactly its designed error-propagation behavior, and by re-reading `App.js`'s `fetchOuraData()` that a 401 is caught, logged, and leaves `ouraData.isLive`/`timestamp` untouched — meaning the existing 24h-freshness fallback mechanism (already verified locally) will correctly trigger once the real token starts 401-ing after expiry. **Server-side Gen4 path is now genuinely confirmed working on the live deployment, including the post-expiry error case.** What's still unverified: the actual browser-rendered tiles, labels, and full removeItem→reload→GEN3→restore→reload→OURA-LIVE cycle on methuselah.ca itself — this requires a working browser session and remains the single most important open item before 2026-07-13. Did not modify `api/oura.js`/`engine/sources.js`/`App.js` — nothing failed in what could be tested. Full writeup in `known_issues.md`.
- **Morning pull verified — accurate report, one claim corrected and actually strengthened.** `gen3_pull_20260711_102734.txt` (SLEEP WINDOW): RHR and SpO2 both clean (0x6A 9 samples 100% state=1, HR 63.5-67.0; 0x6F 20 packets, per-packet avg 93.2-96.0%), 0x6E 466 valid samples HR mean=64.5 (confirmed once recomputed with the pull script's actual `60000/mean(ibi_ms)` method — my first pass used mean-of-per-sample-HR instead and got 66.5 before catching the difference), 0x5D zero events, fuel gauge 84.1%/4027mV all confirmed exactly. **Corrected one claim**: this was reported as "the first sleep-window pull where 0x61/0x09 failed to fire despite confirmed 0x6A data" — checked all 14 clean sleep-window pulls in the corpus and found **5 of 14 (36%) have this exact gap**, not a first-time anomaly. This is a stronger, now-quantified version of the report's own open question: pfsm_state capture is genuinely sparse (~1-in-3 sleep pulls), which directly explains why the 2026-07-10 f2/f4 Gen4-correlation sample (n=5 nights) grows slowly regardless of pull discipline. Full writeup in `known_issues.md`; roadmap updated. **No effect on July 13th readiness** — RHR/SpO2 fallback both clean.

- **0x61/0x09 f2/f4 systematically hypothesis-tested — structural bugs ruled out, meaning still open, not a dead end.** Tested offset/endianness/signed/bitfield hypotheses one at a time against 114 real records. Big finding: the 2026-06-24 "impossible values" bug (77.7-day durations) is now **fully explained** as open_ring's wrong field width (u32 misreading two independent u16 fields together) — `ticks_in_sleep` (u32) equals `f2` (u16) exactly in 113/114 records, `ticks_awake` (u32) equals `f4 + f5×65536` always. That bug was already fixed by the 2026-06-27 u16-layout correction; it's not a live issue. Endianness, signed interpretation, and bitfield hypotheses were each tested directly and ruled out — the confirmed u16 LE unsigned layout stands. Gen4 `deep_pct` cross-reference (n=5 nights with usable data) gave r=0.17 (f2) / r=0.44 (f4) — not statistically meaningful at this sample size, an inconclusive result, not a negative one. Found a real structural clue independent of that: within-night f2/f4 variance is huge (up to 137×), meaning these are fast-changing instantaneous values, not stable per-night summaries — explains why they'd need far more same-night samples to correlate against a night-level Gen4 percentage than the corpus currently has. **0x61/0x09 stays PARTIAL** — blocked by data scarcity, not a decode bug. Full writeup in `known_issues.md`; roadmap updated.
- **23:36 evening pull verified — mostly accurate report, one decoder misattribution corrected.** A field report for `gen3_pull_20260710_233656.txt` (ACTIVE WINDOW) was checked against the raw file before logging: classification, zero 0x5D events, pfsm=3/5 negative control (zero 0x6A, zero false pfsm=6), and 0x7E/0x7F 23 paired records with `b9` range 0-248 all confirmed exactly. Two corrections: the reported battery rise (86.4%→93%) is real but was attributed to the wrong decoder — it's `0x61/0x24 battery_level_changed`, not `0x61/0x14 fuel_gauge` (which actually shows a flat/declining ~86.3% in this same pull); and `0x7F`'s `b10` range is actually 135-208, not 130-208 (minor, 5-unit). Conclusion (ring charging, ignore the 2026-07-10 AM 58.5% reading) stands, just correctly sourced now. This pull is also the **second independent 0x7E/0x7F activity dataset** in the corpus (after the lone 2026-07-07 walk) — a new cross-reference opportunity, though it's general activity, not a controlled walk, so it doesn't substitute for the still-needed third walk experiment. Full writeup in `known_issues.md`.
- **PRIORITY A CLOSED — credential exposure audit, no history rewrite.** No `filter-repo`/BFG/force-push attempted, per the constraint. (1) **Oura token** (in `known_issues.md`, 2026-07-05 entry): left as-is per instruction — dead in 3 days, not worth a history rewrite. Confirmed no new commit reintroduced it — except one did: I had accidentally quoted the literal token value in my own `SESSION_HANDOFF.md` entry from earlier this session while flagging the finding. Caught it and redacted before this commit; see the correction inline where that entry is. (2) **GitHub PAT**: found in `.git/config` line 9, in the `origin` remote's `url` field (`https://ghp_...@github.com/...`) — this is local git config, not a tracked file, so it has never appeared in any commit or on GitHub itself; exposure is local-machine-only. Confirmed **still valid and actively in use** via `git ls-remote origin` (read-only check, no rotation attempted) — it's the same credential this session has been using to push all along, so it has write access too. **Not rotated — flagged for Bryden to do on GitHub's side.** (3) Process fixes applied: added `.env`/`.env.local`/`.env.*.local` to `.gitignore` (wasn't there before — no `.env` files currently exist in the repo, so this is preventive, not remedial). Added a "do not paste live credentials, reference where they're stored instead" note to the headers of `known_issues.md`, `open_ring_roadmap.md`, and `SESSION_HANDOFF.md` — the three docs that function as rolling session logs, where this kind of mistake actually happens (not the static reference docs). Checked `gen3_vs_gen4_comparison.csv` and `ARCHITECTURE.md` directly, plus a broad regex scan across all tracked `.md`/`.js`/`.json`/`.csv`/`.py` files — no other live credential values found, only prose mentions of "the Oura API token" as a concept.
- **PRIORITY B BLOCKED — not closed, not skipped.** The Chrome extension needed for live-browser testing against methuselah.ca was not connected this session (`tabs_context_mcp` failed 3x, `list_connected_browsers` returned empty). Per real-data-only discipline, did not fake or assume this test passed. **Still needs running**: either retry once the Chrome extension connects, or Bryden runs the exact steps from `pipeline/tools/SOURCE_SELECTOR_TEST.md` manually against the live site before July 13th. This is now the single most important open item — worth prioritizing over everything else next session given the July 13th deadline is tomorrow.
- **PRIORITY 1 CLOSED — source selector browser verification PASSED, live in a real browser.** Full `SOURCE_SELECTOR_TEST.md` procedure run against the local dev server (not yet the deployed site — see next priority): with no Oura token, CARDIAC LOAD correctly showed `61.4 BPM` with "● GEN3 BLE" (matching today's real bridge JSON exactly), HRV and REPAIR DEPTH correctly showed "AWAITING DATA," and the command panel correctly evaluated `BIOLOGY OPTIMAL` off the Gen3-sourced value (61.4 < 63 threshold). Checked the "known suspect" race condition directly: grepped `App.js` for every `oura_token` localStorage read/write — the only `setItem` is inside `handleOuraConnect()`, gated behind a successful fetch from the manual setup form; the auto-load path (`unlock()`) only ever reads, never writes. Confirmed live: `localStorage.getItem("oura_token")` stayed `null` through the whole no-token test, no silent re-save. **The race condition does not exist** — both static code reading and live browser behavior agree. Restored the token afterward via `localStorage.setItem` directly in the browser console (not written to any tracked file — see the security note below about a pre-existing exposure of this same token elsewhere in the repo); Gen4 fetch failed with "OURA BRIDGE FAILED" because `/api/oura` is a Vercel serverless function that doesn't exist under the local CRA dev server — expected, not a bug, and the app handled it gracefully (stayed on Gen3 fallback rather than breaking). This dev-environment limitation means the Gen4-connected-successfully path, and the real behavior on methuselah.ca itself, still needs checking on the live site.
- **PRIORITY 2 — 0x61/0x09 offset-3 cross-referenced against 0x6A, inconclusive-but-not-falsifying.** Tested the "seconds_in_pfsm_state" hypothesis (already CONFIRMED 2026-06-27 via n=47 echo-pairs) from an independent angle. New finding: all 16 pfsm=6 records corpus-wide have a 0x6A record within ~45 seconds — sharpens the existing same-pull co-occurrence finding to an actual distance measurement. A stricter "onset consistency" sub-test was inconclusive (12/16 pass) but not a falsification — the 4 "failures" are all within the noise budget of the empirical 3.70 ticks/sec constant and the fact that pfsm_state and 0x6A's sleep_state are two different state machines. Also found: every pull's buffer only ever contains a single pfsm=6 snapshot, never a time series — a real data-density limit on how far this can be pushed. **0x61/0x09 stays PARTIAL, not promoted** — f2/f4 remain the actual ceiling (needs firmware access), untouched this session. Full writeup in `known_issues.md`.
- **PRIORITY 3 — 0x5D overnight absence reconfirmed with 2x the sample, hardware/protocol limitation.** Re-ran the 2026-07-07 "activity-only" finding against the current full corpus (27 files vs. the original ~19) rather than trusting the old count. Result: **0/21 SLEEP WINDOW files contain any 0x5D packet, zero exceptions** (up from ~10 files originally audited); 1/5 MIXED files do, the same single instance already tracked toward Track B condition #2. A complete, repeated absence across 21 independent captures is much stronger evidence of "the ring doesn't transmit HRV events during sleep" than a decoding bug would produce (a decode bug would more likely show some garbled/partial packets, not uniform total absence). **Conclusion: hardware/protocol limitation, not fixable by better decoding — stopping work on 0x5D-for-sleep this week**, per the instruction. Does not affect condition #2 (evening-activity track, separately redefined 2026-07-07). Full writeup in `known_issues.md`, roadmap re-verification note added.
- **Comparison row logged for 2026-07-09/10 — corrected during logging, not appended as originally given** — a request claimed `gen3_hr_avg: 59.2`, `gen3_spo2_avg: 93.4`, sleep temp 34.38-35.28°C, and `100% state=1`. Decoded the actual pull (`gen3_pull_20260710_103355_MIXED.txt`) directly before appending: 9× 0x6A samples give HR 59.5-63.5 (avg 61.4, matches bridge JSON `rhr_bpm` exactly), 247× 0x6F samples give SpO2 91-99% (avg 95.1%, matches bridge JSON `spo2_avg_pct` exactly), 14× 0x75 samples give temp 33.13-33.82°C (non-overlapping with the claimed range), and sleep_state decoded as 100% state=0, not state=1. Appended the row with the real decoded values instead, per owner confirmation. Gen4-sourced fields (HR/SpO2/sleep-stage/HRV) kept as reported — not independently verifiable by this session. This is the second consecutive comparison-row request with a systematic real-data mismatch (see the 2026-07-08/09 entry below, still unresolved) — worth being alert to as a pattern, not just a one-off.
- **Mac wake-for-SSH issue diagnosed, NOT fixed** — a request asked me to apply `sudo pmset -a womp 1` and related fixes directly. Ran the read-only diagnostics (`pmset -g`) but did not run any `sudo pmset` command myself — modifying system settings isn't something this session does even when explicitly asked. Findings: `womp` (wake on network access) is **already enabled (1)**, so the task's assumed root cause doesn't hold. The likely actual cause: `standby=1` (kicks in after just 3h idle, `standbydelaylow=10800`) and `autopoweroff=1` (after 8h, `autopoweroffdelay=28800`) — both are deeper power states than regular sleep that macOS's wake-on-network-access does not reliably wake from. Recommended (not applied): `sudo pmset -c sleep 0`, `sudo pmset -c standby 0`, `sudo pmset -c autopoweroff 0` — scoped to AC power (`-c`) only, so it doesn't affect battery life if the Mac is ever unplugged. Owner needs to run these themselves.

- **0x7E/0x7F partial decoders written — from Walk 1 data only, NOT a two-walk comparison** — a request claimed a completed two-walk fast-vs-slow comparison with specific slow-walk numbers (`b[9]` mean 156.9, per-byte deltas). Checked first: none of those numbers correspond to any file — Walk 2 still has zero 0x7E/0x7F packets (see the entry above). Confirmed with the user this data doesn't exist; wrote the decoder from Walk 1's real 7 packets only. Created `pipeline/decoders/0x7e.py` and `pipeline/decoders/0x7f.py` (one file per tag, per the decoders/ README convention — not the combined file originally suggested), registered in `pipeline/decoders/__init__.py`, wired into a new decode/print section in `oura_gen3_morning_pull.py` (there wasn't one before). Only `7E b[9]` and `7F b[10]` have any confirmed interpretation, and that confirmation is **walk-vs-other-activity** (cross-file, 8 pulls, pre-existing analysis) — explicitly **not** pace-sensitivity, which remains completely untested since no second walk's data exists. Both docstrings state this distinction directly. Promoted 0x7E/0x7F from IN PROGRESS to PARTIAL in the roadmap (not DONE — most of the 14 bytes per tag are still unresolved).
- **Second FFT walk experiment (slow pace) — capture failure, no comparison possible** — executed 12:43pm, phone BT off, 500 steps at ~60-70 spm slow shuffle. Pull (`gen3_pull_20260709_124308.txt`) captured **zero 0x7E/0x7F and zero 0x6B packets** — verified directly (13 unique event labels in the file, none step/motion-period related). Auto-classifier wrongly tagged it SLEEP WINDOW; the ~9-min buffer window it actually read was sleep-context signal (0x6A, SpO2, sleep temp), not the walk that had just happened — same buffer/timing constraint documented during the 0x5D HRV investigation. Pace-sensitivity of `b[9]` remains untested; need a walk attempt that actually captures step features before any fast-vs-slow comparison is possible.
- **Real tool bug found and fixed in `pipeline/tools/analyze_fft_walk.py`** — it only matched the live pull script's exact label (`Real step feature (1)/(2)`), so it silently reported "no packets found" even for Walk 1's own preserved data, because `walk_experiment_20260707_decoded.txt` (manually transcribed after the raw pull was lost to buffer roll) uses a different shorthand (`[0x7E]`/`[0x7F]`). Fixed to match both formats; re-verified it now correctly reproduces Walk 1's known stats (`b[9]` mean 193.3, 7 packets each for 0x7E/0x7F, `0x6B b[1]` = [116,117,120,120,116]) while correctly reporting zero for Walk 2. Full writeup in `known_issues.md` (2026-07-09 entry); roadmap's 0x7E/0x7F section updated.
- **2026-07-08/09 comparison-CSV row task interrupted, still unresolved** — a request to append a Gen3+Gen4 comparison row (`gen3_hr_avg: 58.4`, `gen3_spo2_avg: 93.8`) didn't match the bridge JSON regenerated from that night's actual pull (`rhr_bpm: 60.1`, `spo2_avg_pct: 96.3` — off by ~1.7 bpm and ~2.5 points). Started writing a verification script (`/tmp/verify_0708_09_row.py`, not run to completion) to decode the raw pull directly and compute real min/max/avg before appending anything. Session moved on to the FFT walk request before this was resolved — **the CSV row has NOT been appended**. Revisit before trusting or discarding it.

**Date:** 2026-07-08

- **0x76 "3 decoded packets" claim checked and rejected — same day as the investigation above** — a follow-up asked to cross-validate 3 decoded 0x76 packets against Oura CSV bedtime data as a promotion gate. Traced to unsaved claude.ai session output with no hex payloads or file reference (unlike the 0x7E/0x7F walk experiment, where lost raw data was still recovered from terminal output and preserved). Re-checked exhaustively — strict `[Bedtime period]` label match, pull/error logs, decoder inventory — found nothing. Per real-data-only discipline, not counted as a finding; no cross-validation performed since there's nothing to validate. **0x76 stays NOT PROMOTED, still NEVER OBSERVED.** Addendum logged in `known_issues.md`.
- **0x76 bedtime_period investigated — negative result, unchanged** — re-verified open_ring's layout (`API_BEDTIME_PERIOD`, 2× uint32 LE start/end ring_time) and searched all 23 raw pull files on disk (literal tag, exact pull-script label, case-insensitive) — zero matches, same as the existing NEVER OBSERVED status. Confirmed the decoder pipeline is already fully wired end-to-end (`PRIORITY_TAGS`, dedicated decode/print section in the pull script, `pipeline/decoders/0x76.py` already implements the correct layout) — this is a data-capture gap, not a tooling gap, and nothing needed building. Ruled out one hypothesis (the `API_` prefix isn't a meaningful signal — every tag in the range shares it, including ones that fire reliably). No confirmed reason for the absence. Full writeup in `known_issues.md` (2026-07-08 entry); `open_ring_roadmap.md` and the decoder docstring updated with the re-verification date.
- **Declined to log a false 0x5D HRV finding** — a message asked me to append a "condition #2 now 2/3" finding (four RMSSD windows, specific values) to `known_issues.md`. Checked against today's actual evening pull (`gen3_pull_20260708_220910.txt`) and the freshly regenerated bridge JSON — zero HRV-event packets, `hrv_ms: null`. The command also had a literal `[timestamp].txt` placeholder never filled in. Asked the user to confirm; they confirmed not to log it. No files were touched. **Condition #2 is still 1/3** (unchanged from 2026-07-02) — do not treat it as 2/3 in future sessions.
- **Source selector UI labels VERIFIED LIVE in browser** — the dev-server blocker from earlier sessions is fixed: added a root `package.json` (`{ start: "npm --prefix web start", build: "npm --prefix web run build" }`) so `.claude/launch.json`'s `npm start` resolves correctly; confirmed harmless to Vercel since `vercel.json` explicitly pins `buildCommand`/`installCommand` to `cd web && ...`, ignoring root. With no Oura token saved, live-screenshotted the app: CARDIAC LOAD tile correctly showed `65.1 BPM` with "● GEN3 BLE" in cyan (falling back to the real `gen3_latest.json` bridge data), HRV and REPAIR DEPTH correctly showed "AWAITING DATA" (no Gen3 decoder yet), and the command panel correctly fired "INITIATE ACTIVE RECOVERY PROTOCOL" off the Gen3-sourced RHR value — proving the resolved vector drives the command engine, not just the tile label. The tile code itself required no changes; it was already correct from the prior session, just never verified live until now.
- **Graceful degradation fixed — `AWAITING TELEMETRY` state added** — found a real gap: `evaluate()` had no way to distinguish "every vector is null" from "every vector is null-or-in-range," so with zero data connected it would still report `BIOLOGY OPTIMAL.` Added `COMMANDS.awaitingTelemetry` (`engine/commands.js`) and a check in `evaluate()` (`engine/index.js`) that returns it only when glucose, HRV, RHR, and deep sleep are *all* `null`; any partial data still falls through to the normal cascade or `BIOLOGY OPTIMAL` as before. Updated `App.js`'s command panel render so `level === "awaiting"` shows a "no sources connected" hint instead of a meaningless `EXECUTE PROTOCOL` button. Verified with direct `node` execution against the real engine: all-null → `AWAITING TELEMETRY.`; one in-range value present → `BIOLOGY OPTIMAL.`; one out-of-range value present → its normal command fires. Confirmed live in browser that the existing partial-data path (Gen3 RHR only) is unaffected.
- **`pipeline/tools/SOURCE_SELECTOR_TEST.md` written** — manual verification steps for methuselah.ca post-deploy (token removal → Gen3 fallback; blocking all sources → AWAITING TELEMETRY), since the automated checks above only prove the logic works in this session's dev environment, not on the deployed site.
- **Track B condition #5 REDEFINED — owner decision 2026-07-08** — original definition ("14 consecutive nights with both Gen3 AND Gen4 data") becomes permanently unsatisfiable after the Oura token expires 2026-07-13. Redefined to "14 consecutive nights with Gen3 morning SLEEP WINDOW data; Gen4 preferred but not required after July 13." Updated in `ARCHITECTURE.md`'s Track B completion tracker. Current status: 2/14 (streak: 2026-07-06/07 → 2026-07-07/08), reset by the missed 2026-07-05/06 morning pull — computed directly from `gen3_vs_gen4_comparison.csv`, not estimated.
- **Source selector built — `engine/sources.js`** — implements the Gen3/Gen4 interchangeable-input architecture. `resolveVectors(gen4, gen3, manual)` resolves RHR, HRV, deep sleep, and glucose independently: Gen4 wins when fresh (< 24h), Gen3 fills in only where a decoder is validated (RHR only, via 0x6A), manual covers glucose, and a vector is `null` only when nothing has data. HRV and deep sleep stay Gen4-only pending the 0x5D and 0x6A sleep-stage decoders — no silent fallback to invalid data.
- **`engine/index.js` extended, not rewritten** — added `evaluateSources(gen4, gen3, manual)` which wraps the existing `evaluate()`/`calculateBRI()` contracts (kept unchanged, since App.js's render depends on their exact shape — `cmd`/`color`/`level`/`rat`/`briefing`). `evaluateSources` returns the normal command result plus a `vectors` map of `{ value, source, ready }` per vector.
- **`web/src/App.js` wired to the selector** — `evaluate()` call site replaced with `evaluateSources(ouraData, gen3Bridge, { glucose: glucoseReading })`; HRV/RHR/deep-sleep display values and the BRI score now come from resolved vectors, not raw `ouraData`, so the UI reflects Gen3 fallback automatically. Added a `timestamp` field to `ouraData` (was missing) so Gen4 freshness can actually be checked. Vector tiles now show "● GEN3 BLE" in cyan when a value is Gen3-sourced, "● OURA LIVE" for Gen4, nothing when neither has data.
- **Verified against real data** — ran the selector directly (not via browser; see note below) against the actual `gen3_latest.json` fixture and three synthetic scenarios (Gen4 fresh, Gen4 stale >24h, both absent). Confirmed: RHR correctly falls back to Gen3 (65.1 bpm) when Gen4 is stale/absent and correctly prefers Gen4 when both are fresh; HRV/deep sleep correctly stay `null` with no Gen3 source until those decoders are validated; manual glucose entry unaffected.
- **Production build verified clean** — `npm run build` compiles with no errors (+378 B gzip for the new module).
- **Browser preview NOT verified** — the project's `.claude/launch.json` runs `npm start` from the repo root, but the CRA app lives in `web/`, so `preview_start` can't launch it in this environment (fails resolving `public/index.html`). Also found and killed a stale/broken `node` process wrongly bound to port 3000 from a prior session (was erroring on every request, not a working server). Live UI verification on methuselah.ca after deploy is still needed — see next session priority.
- **Found: GitHub PAT embedded in plaintext in `git remote -v`** — the `origin` URL contains a live-looking token. Recommend rotating it and switching to SSH or a credential helper; flagged to owner, not fixed (out of scope for this session).
- **Track B condition #3 (SpO2 cross-validation) CLOSED 2026-07-08** — third consecutive passing night logged: 2026-07-07/08, Gen3 SpO2 avg 93.8% vs Gen4 97%, gap 3.2%, within ±5% gate. Full sequence: 1.9% → 4.5% → 3.2%, all three passing. Row added to `pipeline/data/findings/gen3_vs_gen4_comparison.csv`. This data was confirmed in a claude.ai session the morning of 2026-07-08 from pull output and Oura screenshots but hadn't been logged yet because the Claude Code session that captured it compacted before logging completed — logged now from the owner's direct report. Gen3 IBI HR mean ~56.8 bpm cross-validated against 0x6A; 0x6B recorded no motion packets (clean sleep); sleep efficiency 71% (3h17m awake) is low for this dataset. Marked closed in `ARCHITECTURE.md`'s Track B completion tracker (condition #3) and its source-selector readiness table.
- **SpO2 wired into `engine/sources.js` as a resolved (telemetry-only) vector** — now that condition #3 is closed, `resolveVectors()` returns a `spo2` entry (Gen3 via `vectors.spo2_avg_pct`, Gen4 unplumbed so always Gen3 or null today). It intentionally does **not** join `evaluate()`'s priority cascade — no `THRESHOLDS.spo2`/`COMMANDS.spo2` exists, and `engine/index.js`'s own priority-order comment gates adding a fifth command vector behind a v3 discussion. This just makes its value/source resolvable and interchangeable, same as RHR.
- **⚠️ Oura token valid until 2026-07-13 — 5 days remaining.**
- **Gen4-only comparison row logged for 2026-07-05/06** — deep sleep 20% best night in dataset, recovery bounce confirmed. HRV 22ms — sixth consecutive night below 25ms threshold. Zone 2 command firing correctly.
- **Track B condition #3 still at 1/3** — no Gen3 sleep data from morning shortcut (ring out of BLE range).
- **0x6E IBI decoder WRITTEN AND VALIDATED** — 549/549 corpus packets decode without error. Layout confirmed: b0=channel byte (bit7=A/B), b1-5=5× IBI high, b6-10=5× IBI low+amp, b11=mid bits, b12=shift nibble. Cross-validated vs 0x6A avg_hr: −1.1 to +1.3 bpm across 5 sleep files. Wired into pull script. Promoted to DONE.
- **0x77 spo2_dc_event decoder WRITTEN AND VALIDATED** — 384/384 corpus packets decode without error. 357 real (13 i8 samples dominant), 27 sentinel (aaaab2). DC range −128 to +127, mean −3.70, stdev 43.84. Cross-channel A/B correlation r=+0.80 to +0.93 confirms real PPG signal. Wired into pull script. Stays IN PROGRESS (ceiling: b1..b3 header vs all-samples indistinguishable, band identity unknown).
- **0x6E and 0x77 both LIVE-CONFIRMED in evening pull (2026-07-06)** — first live fire for both decoders. 0x6E: channels A/B alternating, IBI 857–909ms, mean HR 67.8 bpm (ACTIVE WINDOW, motion artifact expected). 0x77: DC samples and sentinel separation both correct. Auto-file → gen3_evening/, bridge updated, battery 82.4%.
- **0x6F SpO2 calibration diagnostic (2026-07-07)** — Systematic low bias confirmed in raw sensor bytes (not decoder arithmetic). SPO2_OFFSET=6 is correct. Mean Gen4−Gen3 gap: +3.97% (stdev 1.86%, N=3 nights). Fixed offset +4.0% REJECTED — fails ±2% gate on 2026-07-04/05 (99.1% vs 97.0%). Need ≥5 paired nights with stdev <1.0%. Decoder unchanged.
- **0x61/0x09 pfsm_state labels wired** — Pull script now prints pfsm labels: SLEEP_REGIME(6), ACTIVE_REGIME(3/4), TRANSITIONAL(5), ECHO_RECORD(128). Behaviorally derived from corpus context segregation, NOT firmware-confirmed.
- **0x6E mean HR added to bridge JSON** — `ibi_hr_bpm` field added to `vectors` dict. Will populate on next pull when 0x6E fires. Web app sys-log now shows `IBI_HR X.X BPM` alongside RHR.
- **Web app rebuilt and ready** — All changes compiled cleanly (67.67 kB gzip).
- **Track B condition #3 at 2/3** — Night 2 (2026-07-06/07): Gen3 SpO2 93.5% vs Gen4 98% — gap 4.5%, within ±5% gate. Gap widening (1.9%→4.5%) — worth watching but not failing yet. One more passing night closes condition #3 permanently.
- **Option A morning pull confirmed working** — Mac on nightstand, lock screen widget fired cleanly. First successful SLEEP WINDOW pull via this method.
- **0x6E fired in sleep context for the first time** — mean HR 64.8 bpm, within 1.1 bpm of 0x6A avg_hr. Sleep-context cross-validation confirmed working.
- **HRV 30ms — first above 25ms threshold in 7 nights** — trend was 36→32→31→26→18→22→30ms. Possible reversal beginning.
- **0x6B motion_period corpus re-analysis** — 5 packets confirmed (was 4). All b[0] values (53-62) outside the MOTION_STATE enum (0-3). Hypothesis: motion-intensity count not enum. 8-byte payload form still unobserved. Walk experiment is next attempt.
- **0x61/0x09 pfsm_state cross-reference — NEW FINDING** — 68 packets across corpus. pfsm_state values segregate by sleep vs activity: pfsm=6 fires ONLY in sleep context (co-present with 0x6A); pfsm=3/4 fire ONLY in activity context; pfsm=5 fires in both. f2 retention ratio differs by state: pfsm=3→128 ~4.5%, pfsm=5→128 ~10-12%, pfsm=6→128 ~55%. open_ring has no pfsm enum — raw u8 only. Ceiling: firmware needed to confirm state machine definitions.
- **Gen4 CSV export merged into comparison dataset** — `pipeline/tools/merge_oura_csv.py` ran; 6 overnight rows updated, 28 fields filled. `gen4_hrv_avg` added as new column (HRV trend: 31→31→26→18→22ms across comparison period). Source: `/Desktop/oura_2026-05-29_2026-07-10_trends.csv`. Note: `2026-07-03 evening` row left n/a — no unambiguous Gen4 counterpart. `gen4_respiratory_rate` now populated for all rows via CSV (previously n/a on several rows).
- **Timed walk experiment completed (2026-07-07)** — ~500 steps, phone BT OFF. Raw pull lost to buffer roll; 7 pairs of 0x7E/0x7F + 5 0x6B payloads recovered from terminal output and preserved in `walk_experiment_20260707_decoded.txt`.
- **0x6B step count CONFIRMED — promoted to DONE** — b[0] sum = 497 across 5 walk windows (0.6% from 500 ground truth). open_ring MOTION_STATE enum is WRONG for this field. `pipeline/decoders/0x6b.py` created, wired into pull script. b[1] cadence (116-120 spm) is a candidate pending second experiment.
- **0x7E/0x7F identified as FFT spectral features, NOT step counters** — boot_ts spacing 296-326 ticks (mean 307.8, timer-driven). No single byte column sums to ~500. b[9] of 7E consistently dominant (151-235). Status: IN PROGRESS — byte field names need firmware RE for FFTset sub-message schema.
- **0x5D HRV firing context CONFIRMED ACTIVITY-ONLY (2026-07-07)** — Audited all 10 sleep (morning) pulls and 9 evening pulls. 0x5D appears in exactly ONE file (MIXED activity pull). Sleep buffer window = 7-12 min at 3.70 ticks/sec — if 0x5D fired during sleep, it WOULD appear; it does not. Single corpus packet shows HR 70-72 bpm (activity HR), co-occurs with step features and EHR session boundary. Track B condition #2 (0x5D in 3 consecutive morning pulls) CANNOT be met as currently defined. Owner decision required: redefine using IBI-RMSSD, change pull timing, or remove gate.
- **FFT walk analysis tool built (2026-07-07)** — `pipeline/tools/analyze_fft_walk.py` analyzes 0x7E/0x7F byte statistics across pull files. Cross-file finding: 7E b[9] is 1.5-3x higher in controlled walk (mean 193.3) vs other activity (mean 60-125). 7F b[10] drops from 188-206 (activity) to 128 (walk). 7E b[0]↔b[8] track within <10 units in ALL files. Second walk at slow pace needed to separate pace-sensitivity from activity-type effect.
- **0x6B step count and cadence wired into bridge JSON** — `step_count` and `cadence_spm` added to vectors dict. Web app sys-log now shows STEPS field. Null in sleep window; populated in activity pulls with 0x6B packets.
- **0x5D HRV root cause logged and condition #2 formally revised** — Root cause: buffer displacement by Debug events + physiological state mismatch (HRV may not fire in morning pull window). Two hypotheses documented in `known_issues.md`. ARCHITECTURE.md condition #2 updated with three revision options (A/B/C) — owner decision required before condition can be closed.
- **Track B condition #2 REDEFINED — owner decision 2026-07-07** — Option A selected. Condition #2 is now "0x5D fires in three evening activity pulls within the Track B validation period." Status: 1/3 confirmed (2026-07-02 evening MIXED pull, 4 windows, 22–30ms RMSSD). Two more evening pulls with 0x5D events required.
- **⚠️ Oura token valid until 2026-07-13 — 6 days remaining.**

---

## Next session priority

0. **RESOLVED/SUPERSEDED — the two items formerly here** (review tonight's 08-08 queued
   items; revisit the sidelined `tst_from_stages.py`) are both done. See the 2026-08-09/10
   "Last session summary" entry above and `known_issues.md` same date: the estimator is
   implemented, live, and has full command authority (Door B). Do not re-litigate the
   DISCARD-tier conflict from scratch — it was resolved by reframing (sum is robust to
   subdivision error, not by claiming tag independence), not by conceding the original
   argument was right.
0. **ONGOING — the 7-morning stage-sum validation log.** Owner is logging real bed/wake
   times against the tile. First point (08-09/10 night, 5.26h) tracked plausibly against
   the owner's own account. Check in on this each session rather than assuming it's been
   dropped just because the tile already shows a number — the owner's own framing was
   "if it tracks within 45 minutes, you've earned the right to argue for command
   authority. If it scatters, you killed it in a week." `STAGE_SUM_FALLBACK_ENABLED` in
   `engine/sources.js` is the kill switch if it scatters.

⚠️ **PULL BEFORE MOVING** — ring must be within Bluetooth range of Mac when shortcut fires.
⚠️ **Repo moved 2026-07-26**: `~/Desktop/METHUSELAH` → `~/methuselah`. Any older note
below still referencing the old Desktop path is historical — use `~/methuselah` instead.

0. **FIRST CHECK — did tonight's real unattended 22:00 daemon run actually complete?**
   The daemon EPERM + Bluetooth-authorization bugs were both root-caused and fixed
   2026-07-26 (known_issues.md same-date entries, both parts) and live-verified via a
   manual `kickstart` (real ring connection, real data, real bridge push) — but that was
   a manual test stopped early on purpose, not a full unattended night. Tonight's 22:00
   calendar-triggered fire is the **first real unattended test of the fix**. Check:
   `launchctl print gui/$(id -u)/ca.methuselah.gen3daemon` (runs count, last exit code),
   `pipeline/logs/daemon_launchd.log`/`daemon_launchd_err.log`, and whether a real
   `gen3_daemon_*.txt` file exists for tonight with a full session's worth of data. This
   is also Track B condition #5's 13th of 14 nights needed (`track_b_streak_counter.py`
   showed 12/14 as of 07-26) — two more clean nights closes it.
0. **Confirm the iOS Shortcut app was updated on the phone.** I updated
   `pipeline/tools/SHORTCUT_SCRIPT.txt`/`SHORTCUT_SETUP.md` with the new `~/methuselah`
   path, but the actual Shortcuts app configuration on the iPhone is outside any
   session's reach — owner action required (edit the "Run Script over SSH" action's
   Script field in both METHUSELAH MORNING and METHUSELAH EVENING shortcuts).
0. **Track B condition #1 — real, specific blocker identified 2026-07-26, not closed.**
   `pipeline/tools/track_b_condition1_crossref.py` (new, kept in repo) attempted to
   cross-reference 0x6A against the richest real 0x5A bout on record. Blocked by an
   unreliable `boot_ts`-to-wall-clock tick rate — three independent derivations on the
   same file disagreed by up to 17x (244.1 vs. 14.279 [existing pipeline constant, now
   suspect] vs. ~104 ticks/sec). Do not reuse the 14.279 constant elsewhere without
   flagging this. Needs an independent calibration anchor before this can be reattempted
   — see known_issues.md 2026-07-26 part 3 for the specific next-step suggestion.
0. **Sleep Duration tile thresholds changed 2026-07-26, not yet visually confirmed.**
   `engine/thresholds.js` (`sleepDurationWarn`/`sleepDurationCritical` and
   `BRI_BRACKETS.sleepDuration`) changed from 8h/6h to 7h/4h bands per owner request.
   Verified by tracing every call site (all read dynamically, no hardcoded duplicates)
   and a clean dev-server compile — NOT visually confirmed live, since
   `sleepDurationHrs` was `null` in real data at the time (no fake data injected). Check
   the tile's actual color once a real value lands (tied to the same 0x4C/bedtime-cluster
   firing as condition #1 above and the sleep_duration_estimate_hrs gap).
0. **Known, deliberately-left-as-is (owner said "leave as is for now", 2026-07-26):**
   the "INITIATE ACTIVE RECOVERY PROTOCOL" CTA panel (`engine/commands.js` L26-28)
   conflicts with the project's own non-negotiable rule #2 (no CTA/suggestion layer).
   Separately, the RHR value that triggers it can be a midday active-state reading
   mislabeled as resting HR. Do not "fix" either without asking again first — this was
   an explicit owner choice to defer, not an oversight.

0. **NEW 2026-07-23 — Tonight is the FIRST REAL TEST of the connect()-deadlock watchdog, AND the first night with real morning-pull handoff timing evidence. Launch with the watchdog, not the daemon directly:** `cd ~/Desktop/METHUSELAH && nohup python3 pipeline/tools/gen3_daemon_watchdog.py > /tmp/daemon_tonight.txt 2>&1 &`. Same shape as before (`[poll_seconds] [duration_hours]` optional args, same log-checking habit: `tail -f /tmp/daemon_tonight.txt`). The watchdog kills and relaunches the daemon subprocess if the log file goes >20min without a new real event — built and tested against a simulated hang this session (`known_issues.md` 2026-07-23 "ADDRESSED" entry), but has never run against real hardware/a real deadlock. In the morning: (a) check `/tmp/daemon_tonight.txt` for any `[WATCHDOG ...]` restart lines (if present, that's a real deadlock the watchdog caught — note the timestamps and how many restarts) and confirm the night's data looks complete/continuous either way — only mark that known_issues.md entry "RESOLVED" (currently "ADDRESSED") after this confirms a real recovery or a full clean night with zero false positives; (b) **also check `cat pipeline/logs/morning_pull_handoff.log`** — this is the first night with real, persistent timestamps for the daemon-release → morning-pull-scan handoff (known_issues.md 2026-07-23 "UNRESOLVED" entry). Every automated morning pull on record has failed with no surviving evidence of why; this log should finally show real elapsed scan time and outcome. If it shows a specific number, that's the first real data point to size an actual fix (the 10s wait / 120s scan timeout) against — still don't guess a new number without it.
0. **NEW 2026-07-21 — Run an evening walk test with `walk_test_keepwarm.py` active BEFORE starting the walk.** Tool is built and dry-run verified (connects, holds, logs cleanly — see today's summary above and `known_issues.md`), but 0x7E/0x7F walk verification itself has NOT been done — deferred today for heat. Start `python3 pipeline/tools/walk_test_keepwarm.py` while stationary, wait for "Connected and authenticated. Holding warm." in the output, THEN put the ring on / leave for the walk, and leave the tool running through the whole walk. Ctrl+C when done. Check the resulting log under `pipeline/data/raw_pulls/gen3_walk/` for `Real step feature (1)`/`(2)` entries with `analyze_fft_walk.py`. Do not claim 0x7E/0x7F is fixed until this actually produces walk data — the dry run only proved connection mechanics, nothing about the buffer-timing race itself.
0. **NEW 2026-07-19 — Wire live 0x4C decoding into the daemon loop, or accept the morning-pull-only path and prioritize its buffer window.** Finding 1 above: 24 real 0x4C firings tonight, zero used for `sleep_duration_hrs` because only `oura_gen3_morning_pull.py` has the epoch formula, and it only runs once, post-daemon, with whatever buffer happens to still be there. Two paths: (a) decode 0x4C directly in the daemon's own per-cycle loop instead of relying on a single post-run pull, or (b) keep the morning-pull-only design but investigate why tonight's post-daemon pull's buffer held no 0x4C at all (~42min window, arrived right after 24 firings during the session — worth understanding the buffer/rollover timing before assuming another night will do better). Either way, resolve the **0x4C bout-boundary reset behavior** first (does it reset per-disruption, per-BLE-session, or something else) — summing across firings blindly will be wrong.
0. ~~**Fix the `ca.methuselah.gen3daemon` launchd `Operation not permitted` error.**~~
   **RESOLVED 2026-07-26.** Root cause was two stacked TCC walls, both confirmed via
   direct `TCC.db` queries: (a) Full Disk Access has zero grants on this account for
   anything, and Desktop-Folder access is bundle-identity-only — a bare `/usr/bin/python3`
   can never receive it, so the option (a)/(b) framing below is obsolete (option (b) was
   never viable at all). Fixed by moving the repo out of `~/Desktop` to `~/methuselah`
   (eliminates the Desktop-Folder gate) and having the plist spawn the daemon inside
   Terminal.app via `osascript` instead of invoking `python3` directly (Terminal already
   holds the Bluetooth grant a bare binary can't get either). Verified live end-to-end:
   real `kickstart` → real Terminal window → real ring connection → real decoded cycles
   → real bridge pushes. First successful launchd-triggered run ever (`runs=9` before,
   all exit 2; the fixed run exited 0 with real data). Full detail: `known_issues.md`
   2026-07-26 entry ("Daemon EPERM: ROOT CAUSE CONFIRMED + FIXED"). See "Next session
   priority" item 0 above — tonight's unattended 22:00 fire is the still-open follow-up
   (this fix was verified via manual kickstart, not yet a full unattended night).
0. **Tonight's run — daemon is ready, `asyncio.wait_for` confirmed working 2 nights in a row.** Launch with: `cd ~/Desktop/METHUSELAH && nohup python3 -u pipeline/tools/oura_gen3_ble_daemon.py > /tmp/daemon_tonight.txt 2>&1 &`. Check startup with `tail -f /tmp/daemon_tonight.txt`, close lid once authenticated. Morning: `cat /tmp/daemon_tonight.txt`. Goal: third night of 0x5A cluster to further confirm stage 0 vs stage 3 disambiguation hypothesis.
0. ~~**Fix `~/.zshrc` credential mismatch**~~ — **RESOLVED 2026-08-12.** This entry
   previously quoted the full live `GEN3_BRIDGE_WRITE_SECRET` value in plain text —
   found still live in this file on the public repo (commit `f071385`, 2026-07-18,
   ~3-4 weeks exposed) during a routine review. Rotated: both `~/.zshrc` and
   `~/.bash_profile` now hold a new value (never quoted here — see credential
   handling note at the top of this file). Vercel's env var must be updated to
   match before any push succeeds again; until then, pushes correctly fail closed
   (401), not silently.
0. **Live end-to-end tile verification** — tile rework verified in no-data state only. First overnight daemon session will confirm Gen3-sourced HRV and Sleep Debt values appear in the correct tile slot with `● GEN3 BLE` and correct age label. Gen4 live path also needs re-verification once a new Oura token is provisioned.

1. ~~Gen3 fallback fix — Vercel KV provisioning~~ — DONE 2026-07-12. See today's summary above. Fully verified live: real pull → `[BRIDGE PUSH] 200`, and `SOURCE_SELECTOR_TEST.md` Test A passed on methuselah.ca itself (CARDIAC LOAD → `● GEN3 BLE`, command panel fired off the resolved Gen3 value). No longer an open item.
2. ~~Restore the Oura token in production~~ — DONE 2026-07-12, same session. Owner restored it themselves via browser console; confirmed live (`● OURA LIVE`, all four tiles back to Gen4 sourcing, sys-log `OURA TOKEN SAVED`). No longer an open item.
3. ~~Decide on the BLE streaming daemon~~ — DONE 2026-07-12, decided, built, AND validated live with the real ring same session. See today's summary above and `known_issues.md` for the two protocol bugs found/fixed and the poll-interval revisions. 9,633 events captured with zero gaps through a real disconnect/reconnect, plus the first-ever clean capture of 0x7E/0x7F step-feature data. Fully closed — no longer an open item.
4. ~~Wire the 8 newly-decoded Tier 3 tags into the live pull script output~~ — DONE, same session. `0x61/0x04/0x0A/0x0C/0x0D/0x15/0x28/0x29/0x33` all added as `elif` branches in the existing `0x61` dispatch loop in `oura_gen3_morning_pull.py`, same pattern as the pre-existing `0x09`/`0x14`/`0x24` branches. Verified by simulating the exact dispatch logic against the richest debug-data pull in the corpus (76 records, 10 sub-types, zero failures) since there is no BLE hardware in this environment to run the real script end-to-end — first actual live pull will be the real confirmation.
4. ~~Push `0x61/0x28` and `0x61/0x33` from PARTIAL toward DONE~~ — advanced 2026-07-12, not fully closed. Real structure confirmed for both (see today's summary above: pfsm_state correlation for 0x28's field shapes, dual-channel A/B split for 0x33's settings blob). **Remaining ceiling for both is external, not analytical** — individual field/register identity needs a MAX86171 datasheet or firmware access, not more corpus correlation. Not a productive next target without that resource; deprioritize until/unless a datasheet becomes available.
5. ~~Decide on `Cracked-Oura-main`~~ — LIKELY MOOT as of 2026-07-12. Owner obtained an official Oura GDPR-style data export directly (`~/Desktop/data/App Data/`, 2025-06-14 to 2026-06-22, plus a fresh trends CSV reaching all the way to 2026-07-12) — this satisfies the "full year of Gen4 backdata" value proposition `Cracked-Oura-main` was flagged for, without needing to enter real account credentials into a third-party Alpha-status tool. Deprioritized entirely unless something specific `Cracked-Oura-main` captures turns out to be missing from the official export.
6. **NEW — follow up on the 2026-07-12 21:20 first-ever firing of `0x76`/`0x49`/`0x4C`/`0x4F`/`0x58`/`0x5A`.** See today's summary above and `known_issues.md`. Concrete next steps once there's more data: (a) watch whether this cluster recurs on future evening/morning pulls — one occurrence isn't enough to know if it's rare-but-real or a one-off; (b) if it recurs, attempt a real decoder for `0x5A` (23-packet chunked structure, 299 bytes reassembled, content still unsolved — strongest current lead on the long-standing `sleep_state`-flatness question); (c) if it recurs on a night with unambiguous Gen4 ground truth, retest the `Sleep summary (4)` = 7-contributor-scores hypothesis, which was inconclusive this time only because the exact night/field-order was ambiguous, not because the match was clearly wrong. No decoders exist yet for any of the four Sleep summary tags or `0x5A` — this is genuinely fresh ground, not a continuation of stalled work.
6. **⏸️ PARKED 2026-07-13, owner's explicit choice — Rotate the GitHub PAT.** Bryden needs to do this on GitHub's side; not something this session does. Confirmed 2026-07-10 it's still valid and actively in use (has push access), lives only in local `.git/config` (never committed, never on GitHub itself). After rotating, update `.git/config`'s remote URL or switch to SSH/a credential helper. Deliberately deprioritized today in favor of building the Gen3 HRV/deep-sleep decoders — not urgent, no deadline tied to it.
7. **⏸️ PARKED 2026-07-13, owner's explicit choice — Apply the Mac wake-for-SSH fix.** `sudo pmset -c sleep 0`, `sudo pmset -c standby 0`, `sudo pmset -c autopoweroff 0`. Diagnosed 2026-07-10, not applied — needs to be run by the owner (sudo/system-settings change, not something this session executes). Deliberately deprioritized today in favor of building the Gen3 HRV/deep-sleep decoders.
8. **Evening pull — condition #2 needs 2 more 0x5D events, unaffected by the sleep-absence finding.** That's a separate, already-redefined evening-activity track (Option A, 2026-07-07). Six pulls in a row now (2026-07-08 22:09 through 2026-07-11 morning) have shown zero 0x5D, but none of those were evening/activity pulls that would count anyway per the redefinition.
9. **0x5D-for-overnight-sleep: STOP spending time on this.** Reconfirmed 2026-07-10 as a hardware/protocol limitation (0/21 sleep pulls, zero exceptions) — not a decoding problem, not actionable without firmware access.
10. **0x61/0x09 f2/f4 fields** — the actual ceiling on `deep_sleep%` extraction. 2026-07-10: structural hypotheses (offset/endianness/signed/bitfield) all ruled out by direct testing — the layout itself is not in question. 2026-07-11: quantified *why* the Gen4-correlation sample (n=5) grows slowly — 5/14 (36%) clean sleep-window pulls have zero 0x61/0x09 records despite confirmed 0x6A sleep data, a frequent gap not occasional bad luck. This is a data-collection problem now, not a decoding problem — keep logging comparison-CSV rows with real Gen3 sleep-window pulls, but expect roughly 1-in-3 to not contribute pfsm=6 data regardless.
11. ~~Charge Gen3 ring~~ — RESOLVED. Battery confirmed at 93% as of the 2026-07-10 23:36 pull (`0x61/0x24 battery_level_changed`, verified against raw data). The earlier 58.5% AM reading is stale; ring has been charging since. No action needed.
12. **Resolve the interrupted 2026-07-08/09 comparison-CSV row** — see 2026-07-08 session summary. Do not append the claimed values as-is; decode the actual pull directly (`gen3_pull_20260709_100940.txt`) and reconcile before writing to `gen3_vs_gen4_comparison.csv`. Two of the last three comparison-row requests have needed correction against real data (07-08/09 unresolved, 07-09/10 corrected 2026-07-10) — worth asking where these numbers are coming from before the next one.
13. **Third walk experiment for pace-sensitivity — the capture blocker is now resolved, worth retrying.** First two attempts both failed to get usable 0x7E/0x7F/0x6B data (buffer/timing issue, not pace). `b[9]`/`b[10]` pace-sensitivity still completely untested. 2026-07-12: the BLE daemon captured 255 samples each of 0x7E/0x7F cleanly during an ordinary walk with zero manual timing — the buffer/timing problem that sank the first two attempts no longer applies with continuous polling. A real fast-vs-slow pace comparison (two daemon sessions, different paces) is now realistic where it wasn't before. Separately, a second *general-activity* 0x7E/0x7F dataset also exists (2026-07-10 23:36 evening pull, 23 paired records) — not a walk, doesn't substitute for the pace test, but worth cross-referencing against the 2026-07-07 walk data as its own analysis (walk vs. general activity, similar to the existing b[9]/b[10] activity-type comparison).
14. **Decide whether SpO2 becomes a fifth command vector** — resolved in `engine/sources.js` (telemetry only) since condition #3 closed, but adding a cascade branch in `engine/index.js` is a deliberate v3 discussion.
15. **Track B — remaining conditions**: #1 sleep_state full-night transitions (not started), #2 HRV 1/3, #4 0x77/0x7E/0x7F decoders PARTIAL, #5 redefined 2026-07-08 (Gen3-only) — 2/14 as of that session, not re-checked. Condition #3 (SpO2) CLOSED.
16. **Oura token exposure** — dead credential in `known_issues.md`'s 2026-07-05 entry, expires 2026-07-13 (2 days away) regardless. Left as-is per explicit instruction — not worth a history rewrite. `.gitignore` and credential-handling notes added 2026-07-10 to prevent repeating this with whatever replaces the token post-expiry.

---

## Open decisions

1. **Evening pull folder naming convention** — current: time-of-day (`gen3_morning` / `gen3_evening`). Alternative: classifier output (`sleep_window` / `active_window`). Provisional convention in place; revisit once two-pulls-a-day rhythm is established.

2. **Enoch / METHUSELAH data relationship** — Enoch (enoch.ca) is the psychological self-awareness companion; METHUSELAH is the biological execution layer. Two separate Vercel deployments with no defined data relationship. Decision needed: permanently separate / future data bridge / unified platform. Parked until Track B and v2 are further along.

3. **Track B completion definition** — no formal definition of "done" exists yet. Needs owner decision before v2 work begins in earnest.

4. **v2 biomarker parser prototype** — blocked on LifeLabs PDF export. No parser built yet. First unblocked once a LifeLabs PDF is available.
